{
  "name": "kando-global-project",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo build",
    "type-check": "turbo type-check",
    "lint": "node scripts/check-dep-direction.mjs",
    "app:chrome:prod": "cd apps/flutter-app && flutter run -d chrome --web-port=3000 --dart-define=KANDO_API_BASE_URL=https://api.tcgcard.fun/api/v1"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
