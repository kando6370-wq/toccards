# v1.1.0 开发计划

## 1. 依据与优先级

本计划以三份 v1.1 PRD 定义业务目标和验收行为，以 2026-08-12 `dev` 代码、迁移和运行配置证明当前完成度，并吸收《TCG v1.1 PRD 综合评审》的 P0/P1 结论。App 业务不得因为当前代码尚未实现而缩减、改写或降级 PRD；发现不一致时必须记录为待实现差距并按 PRD 收口。

发生冲突时按以下顺序裁决：

1. App 页面、交互、异常和验收行为以 [`TCG Card App v1.1 PRD`](../00-product/TCG_Card_App_v1.1_PRD.md) 为主。
2. Apple ownership、可信证据、生命周期和跨设备规则以 [`Apple Subscription & Premium 权益统一方案`](../00-product/Apple_Subscription_Premium_%E6%9D%83%E7%9B%8A%E7%BB%9F%E4%B8%80%E6%96%B9%E6%A1%88.md) 为上位规则。
3. 订单、通知和 Admin 目标行为以 [`Admin PRD`](../00-product/TCG_Admin_%E8%AE%A2%E5%8D%95%E7%BB%9F%E8%AE%A1%E4%B8%8E%E8%8B%B9%E6%9E%9C%E9%80%9A%E7%9F%A5%E6%B6%88%E6%81%AF_PRD_V1.1_%E8%AE%A2%E9%98%85%E7%9C%9F%E5%80%BC%E6%94%B6%E5%8F%A3%E7%89%88.md) 为主。
4. 当前代码只证明已实现状态。代码与 PRD 不一致时列为开发差距，不以现状削减 PRD。

原始 PRD 保持只读。实现契约、迁移、验收结果和发布边界维护在本目录。每个业务增量必须在同一开发检查点同步记录实现状态、契约或迁移影响、验证结果和未完成边界；文档未同步或必要验证未执行时，该项不得标记为完成。

PRD 条款、实现文件、数据库迁移、自动化测试及外部验收边界统一维护在 [`traceability-matrix.md`](traceability-matrix.md)。

## 2. 当前基线

2026-08-27 官网 Download BUG 修复检查点：根因是首页两处 App Store badge 均为无链接静态 `div`。考虑正式 App Store 地址基本不变且下载入口不应依赖业务数据库，最终按产品决定将当前正式 URL 直接写入两处链接，撤回运行时 `/app-config`、官网代理 Worker 及相关环境依赖；Google Play、App/Workers API 契约、数据库和其他官网交互均不受影响。修复前回归确认 HTML 中正式 URL 为 0 处；修复后 Marketing 1 项回归、Wrangler dry-run、依赖方向、`git diff --check` 及真实本地静态页验证均通过，本地页面正式 URL 恰好 2 处且动态配置引用为 0。Code Review 未发现残留代理、数据库依赖或无关修改。官网已部署为 `toccards-website` version `9816a0b4-9ef5-43f1-96fb-031205f8adcc`；绕缓存线上复核返回 200，正式 URL 恰好 2 处，动态配置引用为 0。

| 范围 | 当前代码事实 | v1.1 差距 |
|---|---|---|
| App 订阅体验 | 已有 Subscription Page、Paywall、Success、StoreKit 2 Fresh Purchase verifier、Secure Storage 补偿队列、本机 Restore 结果分流、App Attest 原生桥接，以及 Performance/1Y/Folder/Scan Waiting 的来源动作恢复；Home/Search/Collection/Profile 顶部入口、Profile Banner 和 Scan 顶部 Pro 次数卡均已接入完整 Subscription Page，功能卡点仍使用 Functional Paywall Bottom Sheet；Profile 顶部及升级 Banner 已同步 Figma `2129:5678` 并按左右 20px 响应式布局，Search/Collection 顶部已同步 Figma `2070:9663` 的标题与皇冠 PRO 胶囊，Search 顶部搜索框、游戏选择框及 Cards/Sets 切换框和 Collection 顶部 Tab、搜索框均已统一为 44px，Collection Portfolio 摘要已同步 Figma `2070:9486` 的 110px 紧凑布局，Home 顶部订阅入口及 Overview/Performance Tab 已同步 Figma `2181:12864`，模式切换器使用固定外框宽度以保持皇冠入口位置稳定；商品局部缺失、15 秒重载、Purchase 状态、首次/冷启动 Premium 三态分流、ATT/Singular 启动顺序及 v1.1 PRD 视觉 Golden 已实现 | 前后台完整矩阵、Singular prod Worker 发布及 iOS 真机验收不完整 |
| Premium 真值 | 鉴权保留可信 `session_id`；Fresh Purchase 与 Restore 均已有独立 Apple 证据、session proof 和 session grant 写链；App 使用 Unknown/Free/Premium 三态及已验证缓存；Scan、Folder、Performance、Home 与 Card Detail 普通历史 1Y 已接入统一授权 | 三态仍缺 iOS 真机前后台与过期续订矩阵验收 |
| Billing 数据 | `0025` 至 `0034` 已覆盖购买链、交易、session grant、原始通知 inbox、生命周期、订单业务事实、USD 汇率及自动续订订单快照；对应结构与 dev 数据已迁入共享 PostgreSQL，34 条 inbox 均归属 `Sandbox` | production 实时 deployment 需在发布任务中重新核验；缺 Sandbox/TestFlight 实单验收 |
| Scan | 服务端已有终身 10 次真源、request ID 原子预占、逐张结算/返还、60 秒租约、响应重放和多设备并发保护；Quota 查询、识别成功及额度耗尽均返回完整 `access/unlimited` 权益字段，Flutter 严格解析并合并本机 Premium 与服务端 Unlimited，完成 Waiting、Quota=0 Paywall、Done 公式、服务端确认 Unlimited 后自动递补、Scan Pro 完整订阅来源返回及 Processing 删除后的后台结算；扫描页在 typed 订阅成功或收到 `ENTITLEMENT_SYNC_REQUIRED` 后主动同步当前 StoreKit 证明，单任务刷新服务端 Quota，只有服务端确认 Unlimited 后才按原图恢复识别；扫描相机页顶部操作栏已收紧为 32px，并保持退出、闪光和搜索按钮垂直居中，状态栏实色背景仅覆盖系统安全区、不再覆盖其下方 10px 间距；扫描定位框以 `390×844` 下的 `top=213px、280×400px` 为基准，结合屏幕尺寸及四边安全区在顶部完整操作区和底部拍照区之间等比缩放，固定保留 16px 间距；可见边框、扫描线、径向暗角中心、Recognizing/Revealing 透明区和实际识别裁剪共用同一个 Rect；剩余扫描次数入口已同步 Figma `1644:6741` 的 48px 提示条、24px PRO 徽章及 13/16 两行文案样式，并置于扫描页最高层级，避免与取景框重叠时丢失点击；顶部及底部操作栏在 Scanning、Recognizing、Revealing 和识别完成切换中持续挂载且保持相同坐标，不再重播入场动画或改变相册/Done 位置；删除相机 Processing Item 会立即释放该 Item 的本地拍照门禁，允许下一次拍照，原识别请求及 Quota 仍按既定契约后台结算 | Sandbox/TestFlight 与真实并发、超时规模验收尚未完成 |
| Folder | 已有 Folder CRUD；Free 总数最多 2 个（含默认 Folder），服务端按当前 session grant 判断，并以单条条件 INSERT 防止并发越限；购买或恢复成功后在原 Folder Sheet 仍有效时打开 Create Folder Modal；服务端并发上限拒绝会先刷新 Folder List 再进入 Paywall，普通失败或权益同步中保留名称输入 | dev 数据已迁入 PostgreSQL 并部署；仍缺 Sandbox/TestFlight 多设备人工验收 |
| Performance | 已有 Home 与 Card Detail 独立服务端接口、六档自然范围、Purchase/Quantity/Folder Move 历史、迁移 baseline、Premium session grant 校验及 App 完整状态渲染；`0031` 已在 dev 真实历史数据的本地副本验证不伪造历史 | dev 最大 owner 仅 24 条 Event、12 个现存 Item，只能证明当前小样本；仍缺重度收藏用户规模、Cloudflare 端到端、Sandbox/TestFlight 验收 |
| Admin | 订单 13 列、11 项组合筛选、动态国家/SKU、10,000 行以内 XLSX 全量导出及 inbox 通知排障视图已实现；订单请求期间查询、重置、分页与刷新均锁定，避免重复请求；完整 Decoded Payload 仅授权用户打开详情时加载，默认不返回 `signedPayload`，复制由用户主动触发 | dev 订单表为空，无法证明有数据及 10,000 行导出时的 3 秒普通查询目标；Sandbox/TestFlight 人工验收尚未完成 |
| 归因与收入 | 已有 Firebase/Mixpanel 基础设施；ATT 首次请求使用独立安装 marker，不借用 Onboarding 完成状态：首次安装仅在 ATT `notDetermined` 时请求，未完成 Onboarding 的后续冷启动不重复请求，存量升级只落 marker、不弹框；随后按最终状态初始化 Singular，回前台只读同步；marker、Key 或 SDK 失败不阻断。Apple verified 新 Purchase 使用 JWS `price/currency` 上报 Firebase 标准 Purchase，并按 `transaction_id` 持久化去重、失败保留重试。统一订阅成功分支另向 Singular 发送 test/production 三套餐事件，不进入 Mixpanel/Firebase，Restore、外部解锁和失败状态不发送；dev Secret 与 `/app-config` 已发布生效，iPhone 11 test Weekly Fresh Purchase 已在 Singular 后台收到 `weekly_cardtest` | 仍缺 test Yearly/Lifetime、prod Worker 与 production 三套餐事件，以及 Sandbox/TestFlight Revenue 验收 |

代码中只有 Scan 使用时触发的相机/相册权限，没有 PRD 所述“首次安装网络授权流程”。本实现选择遵守同一 PRD 的“不得新增无业务需要权限”：不伪造网络授权弹窗。独立安装 marker 可在现有 Splash 内预取，Singular `/app-config` 请求也在此期间提前开始，但该预取不读取 ATT、不请求系统授权、不初始化 Singular；ATT 状态读取和可能的系统弹框仍排在 Splash/启动预加载之后、Onboarding 之前。ATT/Singular SDK、用途说明、首次安装请求、冷启动不重复请求、回前台只同步及非阻断降级均已完成；Singular 配置由 Cloudflare 分环境管理并在运行时下发，test Weekly 真机后台入库已验证，仍待 test Yearly/Lifetime、prod Worker 与 production 三套餐事件验证。

2026-09-03 Singular 订阅事件检查点：iPhone 11（iOS 15.6）test flavor 已从 dev `/app-config` 初始化 Singular。测试先证明旧 IDFA 无法匹配重装后的实例，再从 Singular 本地库确认当前安装 SDID；将该 SDID 注册到 Testing Console 后，控制台收到 `session`。随后切换至新的 Sandbox 账号并完成 `cardx.week` Fresh Purchase，控制台收到 `weekly_cardtest`，App 为 `card ai test`，Bundle ID 为 `com.kando.kandoApp.beta`。启动权益恢复同样产生 StoreKit `purchased` 回调，但因不属于当前购买界面，未重复发送套餐事件，符合既定边界。该检查点只完成 test Weekly；Yearly、Lifetime、production 套餐事件与 Revenue 仍待验证。

产品待决项：评审 P1-B 要求冻结 Lifetime 在 StoreKit 与服务端均不可用时的最长本地兜底时间。最新 App PRD 仅定义“可临时保持 Premium”，没有给出可测试期限；当前代码按该 PRD 持续使用已验证 Lifetime 缓存。该期限会改变离线用户权益，开发不得自行设定，需产品确认后再实现超时转 Unknown 及对应验收矩阵。

平台范围冲突已显式选择最新 Apple PRD：v1.1 App 只在 iOS 激活 Apple Subscription 销售；Android、macOS 和桌面端不读取或激活订阅 SKU，保持 Free 业务可用、权益 Unknown 且不误售/误授权。`subscription-core` 的 Google Play 抽象继续保留，但在产品定义 Android Premium 范围、Google Play 商品和服务端可信 proof 契约前不由 App 激活。该边界的订阅 UI 14 项及实现/测试文件静态分析通过。

## 3. 阶段与验收门槛

### 历史与当前开发检查点（2026-08-13 至 2026-08-18）

- 历史检查点（2026-08-13）：远程 D1 曾连续应用 `0025` 至 `0034` 并确认无待迁移；首次 Worker 发布被 Cloudflare `10021` 拒绝，原因是 Apple 官方库的 `jsrsasign` 依赖在模块全局作用域生成随机值。验签器与 Server API Client 改为请求/定时任务内动态加载后，Workers 类型检查、Apple 权益/Restore/通知/校正定向 43 项及真实 Cloudflare 启动校验通过。dev/test Product ID 后续已确认为 `cardx.week`、`cardx.year`、`cardx.lifetime`。该记录只说明迁移前历史，不授权再次查询或运行 D1。
- 2026-08-17 dev PostgreSQL 切换检查点：App Store Connect Sandbox Server URL 保持 `https://api-dev.tcgcard.fun/api/v1/apple/notifications/v2`；正式 Worker/Admin deployment `6c0697d0-f9bb-423e-8b68-1ab0509e7729`、version `73766f12-d888-4e94-ba2c-f990ef00ec43` 已通过 Hyperdrive 读取共享 PostgreSQL，`APP_ENVIRONMENT=development`、`APPLE_IAP_BUNDLE_ID=com.kando.kandoApp.beta` 与 5 分钟重试保持 dev 边界。34 条迁移 inbox 全部标记为 `Sandbox`；空 JSON 请求仍返回 `400 INVALID_REQUEST`。Secret 内容有效性、真实 Apple Sandbox 通知验签、入库与业务处理仍未端到端验证。prod 只做 deployment/version 只读复核，仍为 deployment `03235e84-694e-4800-854a-650173408412`、version `57213c10-d392-43a9-8d34-c6472fc3febc`，本轮未部署。
- 2026-08-18 Apple Root CA 修复检查点：Apple 官方 `AppleRootCA-G3.cer` 为 583 字节，SHA-256 为 `63343ABFB89A6A03EBB57E9B3F5FA7BE7C4F5C756F3017B3A8C488C3653E9179`。dev 已将其 DER Base64 写入 `APPLE_ROOT_CERTIFICATES_BASE64` 并立即生效，deployment 为 `623ef89f-f01b-48a4-912d-70586538e01d`、version 为 `6985637b-5e4e-480b-b804-19ce02ecef93`；健康检查为 HTTP 200。prod 按不部署约束只创建待部署 version `42f3934f-7cb4-41df-85b5-631b4e4b8954`，当前线上 deployment `03235e84-694e-4800-854a-650173408412`、version `57213c10-d392-43a9-8d34-c6472fc3febc` 和 100% 流量均未改变。Cloudflare 不回显 Secret 值；dev 仍需新 Sandbox 通知验证线上 JWS，prod 待单独授权部署后才能使用新证书。
- 2026-08-18 PostgreSQL-only 当前边界：dev version `f6ca031c-6b96-498d-ac42-acd84e1b7a7e` 承载 100% 流量，存在 `HYPERDRIVE` 且没有 D1 binding。`fetch` 和 `scheduled` 缺少 Hyperdrive 时直接失败；D1 与 PostgreSQL 已完全独立，退役迁移工具只保留历史证据，运行、导入、重试、回滚、数据修复和灾备均不得查询或回退 D1。Flutter 与 Workers 已按 PostgreSQL 的成功空数据、缺价、合法零价和查询失败分别展示与返回；价格实数仍依赖独立上游导入，不得由 D1 补全。
- 2026-08-19 Apple 通知明文恢复本地修复检查点：Sandbox 同一 JWS 离线验签成功、在线验签失败，而原生 Worker `fetch` 访问 Apple OCSP 为 HTTP 200，根因证实为 Apple SDK 3.1.0 所用 `node-fetch` 2.7.0 与 Cloudflare Workers 运行时不兼容；失败发生在结构化通知写入前，因此 inbox 已有原始 JWS 但没有 `apple_server_notification.decoded_payload`，Admin 只能显示受控失败状态。修复仅在 Worker bundle 中将 `node-fetch` alias 到原生 `fetch` 兼容层，补齐 SDK 实际使用的 `Headers`、30 秒超时和 `Response.buffer()`，并保持 `onlineChecks=true`；非 2xx OCSP 响应会立即清理计时器，成功响应在 body 读取结束后清理。未修改数据库 Schema、通知状态契约、Admin API 或未验签数据边界。
- 2026-08-19 本地验证与 Code Review：修复前定向测试因兼容模块不存在而失败；修复后 Windows/PowerShell 本地执行兼容层定向测试 5 项、通知/校正/Admin 影响面测试 65 项、Workers 全量 61 个文件 529 项、Workers TypeScript 类型检查、prod/dev Admin + Worker dry-run build 和限定路径 `git diff --check`，退出状态均为 0；prod/dev bundle 均确认包含兼容层及 Apple OCSP 调用。Code Review 未发现阻断项，确认 alias 范围、超时清理、外部 abort 转发及敏感数据边界与既有契约一致。该检查点没有执行远程数据库写入、Git 提交/推送或 dev/prod 部署；dev 端到端恢复仍需单独授权部署后等待 5 分钟重试，并只读确认原 `processing_failed` Sandbox inbox 已生成结构化通知和 `decoded_payload`。prod 继续保持未部署。
- 2026-08-19 dev 交付与端到端验收：修复提交 `d1c4764aeec17655fac6846636b798dac7f9900b` 已推送至 `github/dev`，并从该提交的干净临时 worktree 重新安装锁定依赖、构建 Admin assets 与 Worker 后部署；本次部署 Version `3dacc5aa-9fd4-4f7e-b560-bb705220d59f` 曾承载 100% dev 流量。部署后真实 Apple Sandbox `DID_RENEW` 回调由该 Version 以 HTTP 200/outcome `ok` 完成处理，Admin 详情确认已写入完整 Decoded Payload。随后同一账号发生并发 dev 发布，当前 100% 流量切换到 Version `fd9719b8-801b-445a-b2c8-86da5ff364d9`；为避免盲目覆盖该并发交付，本任务没有回滚。只读复核确认当前 Version 继续以 HTTP 200/outcome `ok` 处理后续 Apple 官方回调，5 分钟 Cron outcome 为 `ok`，Admin 订单统计已出现后续续期付款记录；`/api/v1/health`、Admin SPA 根路径和 iOS App Config 均为 HTTP 200。该修复不涉及 PostgreSQL Schema migration，prod 未部署。

- 已实现：鉴权保留 live `session_id`、session grant 表与统一查询、Fresh Purchase challenge、StoreKit 2 JWS 官方验签写链、可恢复处理租约、请求幂等/证据摘要审计和新旧生命周期竞态保护；Flutter Fresh Purchase 已尽力申请 challenge、透传 `applicationUserName`，并在返回本机即时 Premium 前尽力将 JWS 持久化到 session 隔离的 Secure Storage 补偿队列，再于后台同步。安全存储故障会显式记录诊断，但不把 Apple 本机 verified 交易改判为购买失败。
- App 本机 Restore 已实现：iOS 调用 `AppStore.sync()` 后只读取 Apple verified `Transaction.currentEntitlements`，按配置 SKU 区分 Success/Not Found，配置 SKU 出现 unverified 时进入 Failed；Subscription Page/Paywall/Profile 使用独立 Restore 结果事件，15 秒 Timeout 与 Failed 共用失败 Alert，Success 不进入 Purchase Success，Restore 期间阻断当前容器且拒绝并发操作。`AppStore.sync()` 的用户取消及其他同步错误均停止本次流程且不读取设备残留 entitlement；取消只结束 Loading、保持操作前权益且不显示结果反馈，其他真实错误进入 Failed。
- Restore 失败诊断已保留非敏感原生错误元数据：iOS `AppStore.sync()` 的用户取消通过专用 `apple_restore_cancelled` 错误码传递且不作为失败记录；其他异常通过 `FlutterError.details` 返回 `NSError.domain/code`，Flutter 记录异常与堆栈并使用既有 Failed UI 和状态判定。日志不包含账号、Product ID 或交易 JWS。iOS 15.5 Sandbox 真机曾复现 `StoreKit.StoreKitError/code 3` 间歇性失败，同一账号的相邻成功操作可取得 verified `cardx.week` 证据并完成 Restore proof，因此该数字码仍按 StoreKit `systemError` 处理，不用于推断用户取消。
- Restore 的 15 秒 Deadline 已移到 `AppStore.sync()` 完成后的 `Transaction.currentEntitlements` 读取阶段；Apple 系统账号/密码交互不再计入 Deadline，避免用户仍在输入密码时提前显示 Failed。同步取消静默结束，其他同步错误及权益读取失败继续显式失败，权益读取仍保留 15 秒超时保护；Android 订阅路径不受影响。
- Restore `systemError` 回退的修复前回归用例稳定复现 `PlatformException(code 3)`，修复后 code 3 + verified 配置 SKU、code 3 + 空权益、非 system error 三个边界及 Apple entitlement 定向 13 项均通过；Restore UI 3 项、Profile Restore 交互 1 项与 Flutter analyze 通过。iOS 15.5 真机确认旧 Sandbox 周订阅已在 `16:46:10` 进入 `EXPIRED`，此时同步成功为空返回 Not Found、code 3 且回退为空返回 Failed；重新购买形成 active entitlement 后连续四次 Restore 均完成 App Attest challenge `201`、Restore proof `200`，末次响应为 `VERIFIED_ACTIVE/cardx.week`，且未产生 Restore Failed 日志。四次有效订阅真机操作未再次命中 code 3，异常分支的直接回归证据来自自动化测试；Android 未运行真机订阅测试，因为 v1.1 未启用 Google Play 订阅证明链。
- PRD 验收 110 的重启边界已有直接回归保护：Secure Storage entitlement cache 只序列化 Apple 已验证权益事实，不保存 Paywall、Restore Loading/Failed、Subscription Success、Purchase Pending 或 `blocked_action` 等瞬态 UI/业务恢复状态。字段类型或日期损坏的合法 JSON 会被整体丢弃并进入 Apple 重新验证，不因强制类型转换中断启动、不误判 Free，也不从损坏数据授予 Premium。该定向测试 8 项及实现/测试文件静态分析通过。
- App Premium 已收口为 `Unknown/Free/Premium` 三态：启动先读取本机已验证缓存，再以不触发 Apple 登录提示的 `Transaction.currentEntitlements` 静默刷新；只有用户主动 Restore 才调用 `AppStore.sync()`。自动续订缓存必须存在未过期 `expiresAt` 才可临时判为 Premium，Lifetime 缓存可持续判为 Premium，过期缓存刷新失败保持 Unknown。Home、Card Detail、Folder 和 Scan 的受限动作遇 Unknown 会先主动刷新；仅明确 Free 才进入 Paywall，刷新失败不提交受限请求、不创建 Folder、不切 1Y，也不伪装成 Free。Profile 同样只在明确 Free 时展示升级入口。该逻辑已有缓存、Reader 及 Scan 三分支意图测试，仍待 iOS 真机前后台与续订过期矩阵验证。
- App 根生命周期已在回前台时通过 `Transaction.currentEntitlements` 静默刷新权益，并读取当前 session 已证明购买链的服务端 lifecycle 校正；不调用用户主动 Restore 的 `AppStore.sync()`，也不导航或自动弹 Subscription Page/Paywall。只有同链且不旧于本机 JWS 的 `BILLING_RETRY/EXPIRED/REVOKED` 才剔除对应 entitlement，再用剩余全部 Apple entitlement 重算；服务端 active 不能单独授予，接口不可用不直接降级。确认 Premium 变 Free 后由当前页面监听状态原地锁定，刷新失败保持现有兜底状态。Flutter 校正/API 定向 14 项、Workers 当前 session/环境隔离的本地 Miniflare D1 兼容层测试及类型检查已通过；该测试不代表运行时读取 D1。仍待 iOS 真机订阅过期、退款和多 entitlement 矩阵验证。
- StoreKit 静默刷新会遍历全部 verified current entitlements：单条损坏证据不遮蔽其他有效权益，Lifetime 优先于可续订商品；没有 Lifetime 时缓存到期最晚的有效订阅。该选择规则已有纯业务测试，仍待 Sandbox 多 entitlement 实单顺序验收。
- Revenue 持久化边界已强化：reported transaction ID 会过滤空值，pending Map key 必须与记录内 `transaction_id` 完全一致；持久化记录只接受非负有限金额、三位大写币种、非空且无首尾空格的交易/SKU，以及 `weekly/yearly/lifetime` plan。损坏本地记录不能绕过幂等、形成永久待重试项或在重启 flush 时伪造收入。实时重复回调、启动 flush 与购买回调并发均按 `transaction_id` 串行去重；Restore、Pending、Unverified 仍不记录收入。Revenue 定向 7 项、ATT 定向 8 项、`subscription-core` 9 项及 Flutter analyze 已通过。
- 首次完成 Onboarding 后和后续完整冷启动已接入一次性启动门禁：权益刷新最长 15 秒，明确 Free 展示完整 Subscription Page，并分别保留 `source=onboarding` 或 `source=cold_start`；Premium 与 Unknown 进入 Home，Unknown 不伪装成 Free。门禁只存在于根启动流程，App 后台回前台或使用中变 Free 不会自动弹 Subscription Page。ATT/Singular 已排在 Splash/启动预加载结束与 Onboarding 之间；仓库不存在产品所述“现有网络授权”，因此按 PRD 禁止新增无业务需要权限的规则不伪造该步骤。
- Subscription Page 与 Subscription Success 的固定内容已按 App PRD 收口：标题分别为 `Choose Your Plan` 和 `You're Premium!`，两页仅展示四项 Premium Benefits，Wishlist 不作为 Premium；Success 主按钮为 `Start Exploring`，不再增加 `Manage subscription` 第二出口。Success 页面样式已同步 Figma `2090:17166` 的 `#070905` 背景、`PREMIUM ACTIVE` 标签、208px 奖杯区域、紧凑权益卡与 56px CTA；2026-08-25 进一步按 Figma Motion 的统一 2350ms 时间轴还原 Premium 标签、双层光环、奖杯弹入、8 个彩屑、标题/副标题、四项权益卡、四个勾选路径绘制和 CTA 的独立透明度、位移、旋转、缩放、错峰及缓动参数。产品契约仍优先于 Figma 的 `loop` 配置：动画只播放一次，效果型光环和彩屑完成后收起，奖杯、文案、权益和按钮停留在完整最终状态；系统 Reduce Motion 直接展示最终状态。失败回归先捕获旧实现缺少独立 Motion 节点，修复后成功页内容/几何/时间轴/不循环/300ms/最终帧 6 项、Subscription Restore/购买来源跳转 20 项和完整静态分析通过；完整 Auth/Profile 文件 63 项通过、2 项既有 Profile Banner 与 Subscription Sheet Golden 差异失败，与成功页代码路径无关。Code Review 未发现阻断项，确认控制器不存在循环调用，购买、权益、来源参数与 `Start Exploring` 既有跳转均未改变；iOS/Android 真机动画全周期仍需人工验收。
- iOS Functional Paywall Bottom Sheet 已同步最新 Figma `1651:9915`、选中态 `2228:18613` 与 Badge 两态 `2228:18625`/`2228:18638`：仅更新 iOS `presentation=sheet` 的 Charizard 卡面背景、权益列表暗面与描边、套餐暗面/选中微光/Radio 和差异化 Badge；背景从 Sheet 顶部开始并承载 Handle，右侧导出亮边由父层超扫裁剪；三个 SKU 统一为 74px 高度、17px 水平内边距和 22px 相邻间距，选中项使用截图确认的 `#38372D` 无渐变纯色 Surface，并保留对应价格/周期文字层级；`MOST POPULAR` 与 `BEST VALUE` 保留各自 SKU 文案，Tag 的背景、描边和 6px 模糊跟随 SKU 选中状态在激活/未激活样式间切换；切换 SKU 使用两段 140ms ease-out，旧项的选中纯色、accent 描边、微光、Radio 和 Tag 完全退场后，新项才开始进入激活态，价格层级使用同一动画规格；购买进入不可点击状态时仍保留当前 SKU 的选中视觉。Android 保持原 Sheet UI，完整 Subscription Page、Success Page、商品状态、购买/Restore、typed success 与来源动作恢复逻辑均保持不变。
- Home/Search/Collection/Profile 顶部订阅入口已按当前产品确认与 `00-product` PRD 统一：仅明确 Free 时显示 Premium 入口，Unknown/Premium 隐藏；四个入口均携带各自 `source` 与 `entry_source=top_subscription_entry` 打开完整 Subscription Page，不再携带 `presentation=sheet`。Profile Banner / `Upgrade Now` 同样改为 Full Page，并保留 `source=profile&entry_source=profile_banner`；Scan 页面顶部 `Pro / X scans remaining / Tap to get unlimited scans` 次数提示卡保持 Full Page，并补齐 `source=scan&entry_source=scan_pro_card`。StoreKit 验证的新购买成功后进入 Subscription Success，`Start Exploring` 按来源返回 Home/Search/Collection/Profile/Scan；`onboarding/cold_start` 和来源缺失返回 Home。来源页面实例与输入状态保留，Subscription Page 和 Success Page 均从返回栈移除。完整成功页已明确展示购买成功，Scan 顶部 Pro 次数卡返回后不再重复显示 `Premium unlocked` 顶部提示；Restore 成功反馈以及 Home Performance、Card Detail 1Y、Capture/Gallery 额度耗尽、Waiting Card 等 Functional Paywall 的购买成功提示保持不变。修改前入口回归稳定读到顶部入口的 `presentation=sheet` 且 Profile Banner `sheet=true`，Scan 定向回归稳定捕获完整成功页返回后的重复提示；修改后订阅入口/启动/成功返回 43 项、Profile Banner Full Page 路由 1 项通过，Scan 91 项中 86 项业务/交互回归通过，仅 5 项既有视觉 Golden 像素差异失败，未覆盖基线；`flutter analyze` 与 `git diff --check` 通过。Code Review 未发现阻断项，确认 7 处 Functional Paywall Sheet 调用保持不变，完整页来源参数和成功页返回栈已由参数化回归覆盖；iOS/Android 真机入口、返回手势与 Sandbox 购买仍需人工验收。
- 当前入口相关验证：共享三态/Full Page URL 契约、Profile Banner Full Page 路由、Scan 顶部 Pro 次数卡来源、Subscription 商品/Restore/来源返回、平台边界、前台刷新及法律外链上下文、Collection 与 Profile 既有覆盖均保留。Home/Search/Collection/Profile/Scan 的 Purchase Success 分别验证 `Start Exploring` 返回原页并保留页面输入状态，cold start 单独验证返回 Home；Full Page 和 Success Page 返回后均不再存在。Terms/Privacy 使用系统外部应用模式；Full Subscription Page 与 Functional Paywall 的直接 Widget 验收已证明打开外链后原订阅容器、非默认 Plan 选择及来源页面状态均保持。StoreKit Product Loading 已实现首次请求加最多 3 次后台重试，默认间隔 1/3/8 秒并共享 15 秒总 Deadline；同一时刻复用单个请求周期，Deadline 或页面销毁后以周期标识拒绝迟到 StoreKit 结果写回。每个新 Subscription Page 实例校验一次商品；同一实例回前台时，已完整加载且真实 SKU、当前 Product ID 配置及 App Store storefront 均匹配的商品原样复用，不重复进入 Product Loading。目录缺失、SKU 配置或 storefront 变化时允许静默补拉且不改变现有页面 Loading；购买前发现所选 Product 无效或购买环境变化时重新加载后再发起购买。任一可用 SKU 返回后立即开放真实商品；未返回项展示 App Store Connect 美国当前价格的固定美元兜底，但保持 StoreKit 不可用且不显示 Selected，不伪造可购买状态。全部失败、购买无法启动及普通交易失败分别使用 App PRD 固定文案，自动重试仅限商品读取，不自动重试 Purchase。Search 既有回归已按当前代码与 PRD 收口，23 项全部通过。2026-08-13 最新全量回归为 App 679 项通过、10 项跳过、1 项失败，`subscription_core` 9 项通过；唯一失败仍是既有 App Shell Golden。该基线最初在测试显式加载 `Geist-Regular.ttf` 时生成，历史提交 `369cd7a` 后字体资源与加载逻辑被删除但 Golden 未同步，Flutter 3.44.7 因此以测试占位字体渲染并产生差异。v1.1 App PRD 未变更底部导航，该问题不作为本期业务需求差距；但在产品/视觉确认恢复 Geist 资产或批准新基线前不得覆盖 Golden，Flutter 全量测试仍不得标记为通过。
- 2026-08-27 Scan 顶部 Pro 次数卡重复成功提示修复：根因是该 Full Subscription Page 入口仍沿用 Functional Paywall 的返回处理，把 Success Page 的 `premiumUnlocked` 返回值再次映射为 `Premium unlocked` 顶部提示。修复前定向 Widget 回归在 `Start Exploring` 返回 Scan 后稳定找到重复提示；现只移除顶部 Pro 卡的该分支，仍执行 StoreKit/服务端权益同步。Restore 返回继续显示 `Premium restored`，Capture/Gallery 额度耗尽及 Waiting Card 等 Functional Paywall 仍显示原购买成功提示。三项边界回归 3/3、订阅入口/启动/Restore 相关回归 43/43 和 `flutter analyze` 通过；Code Review 确认修改未触及 API、StoreKit 验证、路由返回、Quota、Functional Paywall 或 Android/iOS 平台分支。自动化未执行真实 Sandbox 购买，因此两端真机的 Success 动画退场和顶部提示视觉仍需人工补验。
- 2026-08-28 Onboarding 首次订阅成功跳转竞态修复：根因是 `source=onboarding` 的完整 Subscription Page 收到 `purchaseSuccess` 并开始进入 Subscription Success 后，路由切换完成前仍会消费紧随其后的 active entitlement 结果；后续结果被控制器归类为 `externalPremium` 时会覆盖成功页导航，直接进入 Home 并显示 `Premium unlocked`。现仅在 `source=onboarding` 且非 Sheet 的页面实例收到 `purchaseSuccess` 后锁定本次成功页导航，后续终态结果不再改道；用户必须在 Subscription Success 点击 `Start Exploring` 才进入 Home。`cold_start`、Home/Search/Collection/Profile/Scan、Functional Paywall、Restore、StoreKit 验证、Premium 真值和服务端同步逻辑保持原处理。修复前同帧注入 `purchaseSuccess` 与后续 Premium 事件的回归稳定得到 0 个 Success Page；修复后完整验证 Success 保留、无重复解锁提示且按钮进入 Home，启动门禁与 Subscription/Restore 相关 36 项及 `flutter analyze --no-pub` 通过。Code Review 未发现阻断项，确认保护标记不会在 cold start、其他完整页来源或 Sheet 中置位，也不改变权益更新、收入记录、缓存和服务端同步。自动化未执行 iOS/Android 真机 Sandbox 购买，真实 StoreKit 回调时序仍需人工补验。
- 2026-08-28 Premium 过期原地重锁修复：服务端 Apple Notifications V2 已正确把 Billing Retry/Expired grant 标记为 `expired`、Refund/Revoke 标记为 `revoked`，但 App 持续在前台时仍可能保留旧 Premium；受保护接口因继续收到 `X-Local-Premium-State: verified` 而返回 `409 ENTITLEMENT_SYNC_REQUIRED`，原 Home 修复只同步 session grant，不会重新读取 StoreKit 或把全局状态降为 Free，Card Detail、Folder 与 Scan 也没有统一收敛，其中 Scan Quota 失败还会保留旧 `unlimited=true`。现增加全局单任务权益对账：精确 409 后先静默读取全部 verified `Transaction.currentEntitlements` 并应用当前 session lifecycle，确认有效才同步 proof 并让原请求最多重试一次，确认失效则更新 Free 缓存和全局状态并驱动 Home/Card Detail/Folder/Scan 原地上锁，验证不可用不误降级、不重试；Scan 确认 Free 后重新读取服务端 Free quota，清除旧 Unlimited。已验证订阅同时按 `expiresAt` 安排一次性前台静默复核，不调用 `AppStore.sync()`。修复前控制器回归因缺少对账 API 失败；修复后对账三态/并发/前台到期 5 项、Collection 与 Scan 过期 2 项、Card Performance 过期 1 项及相关 Home/Card Detail/Collection/Scan 定向回归通过。服务端、购买/Restore、成功页、收入上报、API/Schema、Android 订阅证明链均未修改；iOS Sandbox 前台自然过期、退款和自动续期真机矩阵仍需人工补验。
- 2026-08-27 完整 Subscription Page 顶部改为 edge-to-edge：视频背景从屏幕 `top=0` 开始绘制并覆盖刘海/灵动岛状态栏区域，不再由页面 SafeArea 在顶部留下独立暗色带；关闭按钮、Restore 和全部滚动正文继续位于 SafeArea 内，顶部操作仍以安全区底部向下 12px 为布局基准。Functional Paywall Bottom Sheet 的高度、安全区、Handle 和 iOS/Android 背景选择保持不变。修复前 390×844、59px 顶部安全区回归稳定测得背景 `top=59`，调整后背景为 `top=0`，关闭按钮与 Restore 的顶部坐标均不小于安全区底部加 12px；该定向回归、宽屏视频适配、双平台 Sheet 背景及 Subscription/Restore 31 项通过。未修改 Subscription Success、StoreKit、购买/Restore 结果、来源返回或 API；刘海屏与灵动岛真机的视频首帧和系统状态栏图标对比度仍需人工验收。
- 2026-08-27 Subscription 四项权益样式同步 Figma `2090:17443`：完整 Subscription Page 与 Functional Paywall Bottom Sheet 共用同一 `_BenefitRow`，每项固定 50px 高、4px 间距、8px 圆角、`rgba(26,28,20,0.4)` 背景、`#2A2D20` 描边和 2px 背景模糊；内部使用 13px padding、24px `accentGlow10` 圆形底、Figma 原始导出的 `9.50833×7.01458` 勾选 SVG，以及 14/20、Regular、主文字色的单行文案。修改前完整页仍使用另一套暗面/描边且两种容器都使用 15px Material 勾选图标；修改后 Full Page 与 Bottom Sheet 两项共享样式回归、Subscription/Restore 31 项通过。权益文案、页面/弹窗尺寸、套餐、购买、Restore、Success 和来源返回不变；iOS/Android 真机的背景模糊、字体渲染和视频叠加仍需人工验收。
- 2026-08-25 Subscription 默认套餐跨容器残留修复：修复前回归稳定复现冷启动完整 Subscription Page 选择 Weekly 并关闭后，从 Home 新建 Functional Paywall 仍继承 Weekly。根因为 `selectedPlanId` 存放在共享 Subscription Controller，而页面关闭只结束购买展示、没有建立新的套餐选择生命周期。现在每个新 `SubscriptionPage` 实例仅初始化一次套餐选择，Yearly 可用时恢复默认 Yearly，Yearly 不可用时沿用首个可用 SKU 回退；购买、Pending、Restore 期间不改动选择。同一容器内主动切换、rebuild、Terms/Privacy 返回、前后台商品刷新、商品价格、权益、购买和来源跳转逻辑保持不变。修复前新增用例按预期失败并得到 `weekly`，修复后 Subscription UI 21 项、订阅与启动门禁相关 66 项及 `flutter analyze` 全部通过。Code Review 未发现阻断项，确认新容器初始化只写套餐选择、页面销毁后不迟到写入，并且不影响既有订阅成功动效改动；iOS/Android 真机仍需按原冷启动到 Home 皇冠路径补验。
- 2026-08-25 Subscription SKU 展示兜底按产品最新决定更新：App Store Connect 登录态核验 dev/test App `Kando KP App` 的美国当前价格为 Weekly `$3.99`、Yearly `$49.99`、Lifetime `$79.99`。Subscription Page 与 Functional Paywall 共用该固定美元展示兜底，单个 StoreKit SKU 缺失时只补对应价格；真实 StoreKit 本地化价格优先覆盖。兜底不写入 `availablePlanIds`，不伪造 StoreKit 商品、不绕过购买前重新查询，也不参与实际扣款或收入上报。iOS 15.5 真机当前三项 SKU 均正常返回韩国商店本地化价格 `￦6,600`、`￦77,000`、`￦129,000`，因此真机页面未触发美元兜底。修复前两项缺失/部分缺失回归分别稳定找不到 `$49.99` 与 `$3.99`；修复后 Subscription UI 22 项、`flutter analyze --no-pub` 与 `git diff --check` 通过。关联 Subscription/Auth 定向验证 7 项通过，既有 Subscription Sheet Golden 仍有 1.97% 像素差异，未覆盖基线。Code Review 发现并修正首版会把 App Store 美元兜底泄漏到未启用订阅销售的 Android/未配置目录环境，复审确认真实价格覆盖顺序、StoreKit 可购买集合、购买前重载、收入上报及 iOS/Android 边界未改变。
- 2026-08-26 Subscription 购买反馈已按真机 Sandbox 行为细分：StoreKit 账户验证、系统错误、网络、付款方式、购买限制、App Store 条款、地区、优惠、商品不可用、重复交易、购买回调中断、未知商品、交易验证失败、Pending 与 Cancelled 分别使用明确顶部提示；优先结构化读取 StoreKit 原生 domain/code，未识别错误显示清洗并截断后的错误码，完整异常仅保留在诊断日志。Cancelled 不再静默，明确说明购买未完成且未扣款。Subscription Page、Functional Paywall 及其法律链接失败的顶部提示统一显示 5 秒，仍允许手动关闭；Purchase 不自动重试，失败后恢复原订阅容器和可点击状态。修复前新增回归因分类函数和文案常量不存在而编译失败；修复后 Subscription/Toast 定向回归 34 项及 `flutter analyze` 通过。Code Review 补充了 `PlatformException.details` Map 中原生错误码的匹配与展示；该 Flutter 逻辑保持跨平台可编译，v1.1 Android 订阅销售仍未启用。iOS Sandbox 真机的各错误分支及 5 秒视觉时长尚需补验。
- Profile 非订阅 Banner 已同步 Figma `2210:17750`：保留 152px 高度、响应式页面宽度、现有标题/说明/按钮与 `profile_banner` 订阅来源，仅将背景替换为卡牌、票券和上升箭头插画，并按设计稿右对齐放大裁切；Premium/Unknown 仍不显示该入口。Home、Search、Collection、Profile 四个普通 Tab 的主内容均随设备可用宽度伸展并统一保持左右 20px 页面边距；Profile 不再把 390px 设计基准作为根内容最大宽度，Account、Customer Support 等非 Tab 二级操作页仍保留 390px 最大内容宽度。Banner 视觉 Golden 与 Free/Premium 显隐、尺寸、跳转及宽屏布局回归覆盖本次变更。
- 前台商品刷新新增 1 项上下文失效回归：页面关闭后，迟到 StoreKit 结果不得写回商品目录，且同一刷新周期不再继续重试；本段 Subscription 定向测试现为 16 项通过。
- Profile Restore Blocking Loading 已有直接 Widget 验收：Restore 期间全页业务点击由 `AbsorbPointer` 吸收，系统返回由 `PopScope` 拦截，并持续显示全屏 Loading 遮罩；点击 Customer Support 不会导航，符合 App PRD 第 99 条。
- Restore proof 代码已实现：iOS `DCAppAttestService` 注册安装级 key，对 Workers 返回且绑定 live session、一次性 nonce、request ID、key ID 和 StoreKit JWS 摘要的 canonical client data 签名；Workers 使用 WebCrypto 验证 Apple 证书链、nonce、App ID、AAGUID、key ID、assertion 与递增 counter，原子消费 challenge 后只为当前 session 写 `source=restore` grant。同 UID 另一 session 盗用与重放已通过本地 Miniflare D1 兼容层集成测试拒绝；该测试不代表运行时读取 D1。
- Fresh Purchase 补偿队列错误码已收口：有效 60 秒处理租约返回可重试的 `VERIFICATION_UNAVAILABLE`；证据/idempotency 冲突和较新 lifecycle 覆盖返回终态 `STATE_CONFLICT`。客户端仅保留临时失败，终态冲突移除并继续后续交易，避免永久阻塞队列。
- Notifications V2 已实现原始收件箱、官方 JWS 验签、结构化消费、通知/交易幂等、生命周期乱序保护、5 分钟失败补偿及 Apple Server API 当前状态校正，并通过本地 Miniflare D1 兼容层集成测试；该测试只验证 SQL/业务契约，不是运行时数据源证据。Scan/Folder/Performance 权限已接入；App 的功能卡点使用 typed Paywall success 和页面实例内的最小上下文恢复原动作，不持久化旧动作，Close/Cancelled/Pending/Failed/Restore Not Found 不执行，目标失效时不强制恢复。Subscription 商品优先展示 StoreKit 本地化价格，单个 SKU 缺失时仅展示固定美元兜底且不阻断其余真实商品，Yearly 缺失时选中首个可用商品；购买前商品重载共享 15 秒 Deadline，Pending 禁止重复购买和 Restore 但允许关闭，关闭后的迟到 verified 更新只刷新 Premium，不恢复旧成功流。App Attest 尚未在 macOS/Xcode/真机验证；dev Apple Server API Secret 配置项已存在，但内容有效性和真实调用尚未验证，production 需独立配置。`0026` 至 `0029` 是远程 dev D1 的历史迁移记录；当前运行结构以共享 PostgreSQL 为准，常用 local 与 production 状态需独立核验。因此阶段 A/B 的代码已形成闭环但发布验收未完成，不能视为 P0 或 v1.1 完成。
- Folder 限制已接入统一授权：服务端仅认可当前 live session grant；Free 总数最多 2 个且包含默认 Folder；条件 INSERT 保证同 owner 多 session 并发不能创建第 3 个。本机 Premium 已验证但 grant 未同步时，客户端只发送状态提示，服务端返回 `ENTITLEMENT_SYNC_REQUIRED` 且不创建。App 在本地明确为 Free 且已有 2 个 Folder 时进入 Functional Paywall；购买或恢复成功且原 Folder Sheet 仍有效时自动打开 Create Folder Modal，非成功结果保持原状态。
- Folder 的服务端并发拒绝反馈已按 App PRD 收口：Create Folder 请求留在弹窗内执行，Saving 时锁定输入、返回和重复保存；成功才关闭。普通失败沿用现有错误反馈并保留名称，`ENTITLEMENT_SYNC_REQUIRED` 保留弹窗并提示 Premium 正在同步；另一设备抢占最后 Free 名额导致 `PREMIUM_REQUIRED` 时，Controller 先刷新 Dashboard/Folders，再关闭 Create Folder、打开 Functional Paywall。Paywall 成功后以原名称重新打开 Create Folder，未成功则不创建。Flutter Collection Controller/Widget 50 项及 Workers 本地 Miniflare D1 兼容层 Folder 集成 3 项已通过；该测试不代表运行时读取 D1，仍待真实多设备人工验收。
- Collection Item Folder Move 复用现有单次原子 `PATCH` 保存目标 Folder 与编辑字段，保存中禁止重复提交，API 网络 Deadline 短于 PRD 的 15 秒上限；失败或 Timeout 不更新本地 Item。目标 Folder 已被另一设备删除并返回 `NOT_FOUND` 时，App 会刷新 Card Detail 可选 Folder 及全局 Collection/Home/Search 数据，Item 仍保持服务端 Source Folder；Item 仍存在时保留其他编辑输入并把 Folder 草稿恢复为真实归属，Item 也已失效时退出编辑态。Home Performance 已修正为按 Item 最新 Folder 归属全部可靠历史：Move 后 Source 当前及历史全部移除，Target 从可靠起点获得完整曲线，Quantity、价格映射和 Delete 时点仍按历史 Event 还原。Card Detail Controller 39 项、Controller/Widget 合计 77 项、Workers Item 路由 12 项及 Portfolio 10 文件 57 项已通过。
- Scan Quota 服务端闭环已落地 `0030`：`scan_quota_request` 是额度与幂等唯一真源；App 在哈希完成后通过 `POST /scan/quota/reserve` 按 Queue 顺序逐张原子预占并立即刷新 `{access, unlimited, limit, reserved, consumed, remaining}`，识别请求再以同 request ID 原子领取 reservation，旧客户端仍可在识别入口直接预占。各 Item 预占顺序固定但 OCR 可并发，预占与识别共享原单次 15 秒网络 Deadline；配置、payload 或权益同步使 OCR 无法启动时只释放 `attempts=0` 的未处理 reservation。Matched/No Match 消耗，技术失败释放，Premium 不写 Free 消费。60 秒 lease 过期后不再计入额度，迟到 Worker 不能重新结算消费，同一 request 的并发接管仍只有一个获得处理权。Flutter 缺少权益字段时显式失败，进入页面后首帧拉取服务端真值，并以本机 Premium 或服务端 Unlimited 的合并结果控制展示、额度拦截和请求保护；本机为 Free 时在页面生命周期内至少复核一次 StoreKit，避免旧 Free 状态直接消费额度。游客注册为新用户时迁移其已结算 `free/consumed` 次数，登录已有用户不合并；客户端 Timeout 或传输结果不确定时由 Failed Retry 复用原 request ID，避免迟到成功与新请求重复消费。Gallery 超额和 Failed Retry 可转 Waiting，单张 quota=0 不留 Item，Waiting 删除/Functional Paywall、Done 唯一公式及按 Queue 顺序自动递补已实现；普通或已删除 Processing 技术失败都会刷新服务端 Quota 并递补 Waiting。Capture 与 Gallery 创建前统一检查 10 张 Queue 上限。只有 Waiting 来源的 Paywall typed success 会恢复仍存在的 Waiting；quota=0 且图片未入 Queue 时成功后只返回 Scan。Scan Pro Card 进入完整 Subscription Page，Purchase Success 经 Success Page 返回原 Scan 实例，Restore/外部解锁直接返回，并在有效 Queue 仍有 Waiting 时递补。Unknown 主动刷新失败显示顶部失败提示且不误扫/误弹 Paywall；Premium Scan 显示 `Unlimited scans`。`ENTITLEMENT_SYNC_REQUIRED` 保留为独立同步等待状态，只有服务端确认 `unlimited=true` 后才自动提交；明确 Premium→Free 或 Unlimited→Free 时恢复原 Free Remaining。代码闭环已完成，Sandbox/TestFlight 与真实 PostgreSQL 并发、Worker 中断、弱网迟到响应验收仍待完成。
- App PRD 验收 125-128 已形成直接自动化证据：本地 Miniflare D1 兼容层测试证明两个 session 并发创建 Folder 只能一个成功且总数保持 2；该测试不代表运行时读取 D1。ATT 测试证明后台恢复只读取状态并同步 Singular、不再次请求授权，未完成 Onboarding 的后续冷启动也由独立安装 marker 阻止重复请求；存量安装升级只认领 marker 并同步当前状态，不主动弹框。Scan Widget 测试分别证明 quota=0 的 Capture 与 Gallery 在 typed Premium success 后只返回原 Scan 页面，`photo`/`library` 调用次数仍为 0，用户必须重新操作。当前 Windows 完整 Scan Widget 套件 91 项行为通过，4 项既有 Golden 像素基线失败；Folder 兼容层集成 3 项、ATT 8 项及本次 Scan 定向 `flutter analyze` 通过。这不替代 iOS 真机 ATT、Sandbox/TestFlight 和真实多设备验收。
- App PRD 验收 129-132 已补齐生产规则和直接测试：只有 Waiting 来源的 typed Premium success 会刷新服务端 Quota，并在确认 `unlimited=true` 后按原图重提为 Processing；Restore Not Found 按最新 verified current entitlements 明确归约为 Free，Restore Failed/Timeout 不改变原 Premium 三态；Pending 同时由 Subscription Page 按钮禁用和 Controller 入口守卫阻止重复 Purchase。Scan Waiting 定向测试与 Subscription/Restore 13 项测试已通过；仍需 Sandbox/TestFlight 验证 Apple Pending 最终回调、真实 Restore Not Found/Timeout 及 Waiting 重提链。
- App PRD 验收 133-136 已完成代码核对和直接测试：Apple Pending 状态跨过 16 秒后仍保持 Pending，不套用商品读取/Restore 的 15 秒失败规则；Collection Item 的字段编辑和 Folder Move 由同一次 PATCH 及 PostgreSQL 兼容数据库批处理原子保存，目标 Folder 不存在或越权时不执行 batch、Item 保持原 Folder 和原字段；Move 成功后 Card Detail 立即使用服务端返回的 Target Folder，并统一失效 Home、Collection、Search 及 Home Performance。核对时发现 Home Performance 原先未纳入资产变更失效范围，现已补齐，避免继续显示 Source/Target Folder 的旧缓存。Subscription Pending 定向测试、Card Detail Controller 40 项、Workers Item 路由 12 项已通过；真实弱网 Timeout、跨设备删除和远端 Performance 重载仍待人工验收。
- 全局 Deadline 复核发现 Scan Dio 原先使用 4 秒连接与 12 秒响应两个独立上限，最坏可超过 App PRD 的单次 15 秒总 Deadline。现已在 Scan API 调用边界从请求发起时统一计时，覆盖 Quota、识别和确认；到期主动取消客户端等待并归约为 `REQUEST_TIMEOUT`，晚到成功不能更新已过期操作。识别的 `request_id` / `Idempotency-Key` 与服务端预占、结算、响应重放保持不变。短 Deadline 自动化测试已补充；真实弱网下服务端最终结算与 Waiting 递补仍需 Sandbox/TestFlight 验收。
- Portfolio 公共 API 已同步收口 15 秒总 Deadline，覆盖 Folder、Collection Item、Wishlist、Dashboard 与估值历史读写；Timeout 后取消客户端等待并使用 PRD 通用文案，晚到写响应不能完成已过期 UI 操作。Create Folder、Quick Collect、完整 Collection Item Create 与 Add Wishlist 已补齐客户端与服务端幂等闭环：Timeout 后按稳定 owner 与完整操作语义复用 UUID Key（access token 刷新不影响），服务端同 Key/同语义重放原资源，同 Key/不同字段冲突，非法 Key 拒绝，成功后新操作生成新 Key；两个 Item 创建入口使用独立操作域。Folder Move 仍为单次原子 `PATCH`，现有 Controller 的请求代次判断继续隔离晚到读取结果。Workers Folder/Item/Quick Collect/Wishlist 4 文件 38 项、Flutter Portfolio 21 项、Workers type-check 与 Dart 定向静态分析已通过；真实弱网响应丢失仍需设备验收。
- Card Data 公共 API 已同步收口 15 秒总 Deadline，覆盖 Home 推荐、Search、Set/Card Detail、市场价和普通/批量 Price History；到期取消客户端等待并归约为 `REQUEST_TIMEOUT`。Search 与 Range 仍由现有 Controller 请求代次隔离旧响应，新增短 Deadline 测试证明迟到 Catalog 成功不会完成过期查询。真实快速连续搜索、切 Range 与弱网恢复仍需设备验收。
- Auth API 已收口 15 秒 Deadline，覆盖匿名会话、邮箱登录/注册、验证码、找回密码、OAuth 回调、Token 刷新、Logout 与删除账号；启动 `validateStoredSession` 的 `/auth/me → token refresh → /auth/me` 串行链从操作开始共享同一预算，每一步只使用剩余时间。Timeout 取消当前客户端等待并复用现有网络失败处理，迟到登录或 session 校验成功不会返回 session，OAuth 仍使用既有授权失败文案。单请求与三段校验链短 Deadline 测试已补充；系统浏览器中的 OAuth 交互本身仍按 PRD 例外。
- Currency Rate API 已收口 15 秒总 Deadline；并发选择继续复用单个 `/rates` 请求，Timeout 后保持旧货币、不缓存迟到结果并清理在途 Future，使下一次主动选择可以重新请求。自动化测试直接证明两次超时选择会发起两次独立请求；真实弱网货币切换仍需设备验收。
- Apple entitlement Workers HTTP 已收口 15 秒 Deadline，覆盖 purchase challenge、Fresh Purchase JWS 同步、App Attest challenge/register、Restore proof verify 与 lifecycle 校正；Timeout 归约为可重试 `REQUEST_TIMEOUT/408`，迟到 grant 响应不会完成过期请求，补偿队列继续保留证据。Apple Purchase Sheet、`AppStore.sync()` 与 App Attest 原生计算不套用该 HTTP Deadline，本机 StoreKit verified 即时 Premium 不等待 proof。定向 API 7 项已通过；真实弱网补偿、App Attest 真机和 Sandbox 仍待验收。
- App PRD 验收 137-140 已补齐直接 Widget 证据：Processing Item 删除后立即从可见 Queue 移除，`Done` 只按当前可见 Item 重算，在后台识别/Quota 请求尚未完成时即可恢复；后台最终成功仅更新服务端 Quota，不把已删除 Item 插回 UI，技术失败会刷新 Quota 并按 Queue 顺序递补现有 Waiting。Functional Paywall 关闭或其他未返回 typed Premium success 的结果不会 retry Waiting，也不会执行来源动作；各功能统一以非空 typed success 作为恢复门槛。Scan 定向测试已通过，真实 Worker 中断、租约接管和多设备时序仍待 Sandbox/TestFlight 验收。
- 2026-08-20 Scan 页面状态回归修复：顶部和底部操作栏原先在进入 Revealing 时临时套入新的 Opacity/Translate 子树，导致控件销毁、重建并再次出现；相机拍照门禁原先是与 Queue Item 无关的全局布尔值，删除 Processing Item 后只能等待旧识别结束。现已让操作栏在状态切换中保持同一挂载层级，并把相机门禁及拍照反馈绑定到具体 Item ID；删除只释放该 Item 的本地门禁，旧请求继续后台结算，且旧请求迟到完成不能误释放新请求。修复前两条回归测试分别稳定暴露控件重建和删除后无法再次拍照，修复后定向 2 项及完整 Scan Widget 中 77 项行为测试通过，`flutter analyze` 通过。完整文件的 5 项 Golden 对比仍失败，差异包含未经过本次相机代码的 Review 页面且集中于文字/图标像素；本次没有更新 Golden 基线，需在统一渲染环境复核后补验。Code Review 未发现阻断项，确认旧请求迟到不会清除新 Item 门禁、图库 Item 不会误释放相机门禁，且删除路径仍保留 `_pendingScans` 后台结算。未修改 Workers API、Quota、订阅、识别结果或 Processing 后台结算契约。
- 2026-08-20 Scan 底部操作栏位置返工：上一轮已消除 Revealing 入场动画重建，但 Revealing 仍把底部偏移从 `22px` 改为 `19px`，并同时切换两侧控件宽度、水平 padding 和纵向对齐，导致相册按钮向右 `3px`、向上 `5px`，Done 也发生位移，识别完成后再恢复。现已固定使用拍照前的底部 `22px`、两侧 `72px`、无额外水平 padding 和底部对齐。回归测试直接比较拍照前、Recognizing、Revealing 和最终完成后的 Gallery/Done Rect，修复前稳定失败、修复后通过；相关定向 3 项、完整 Scan Widget 中 77 项行为测试及 `flutter analyze` 通过。5 项 Golden 仍未通过，其中 Revealing 包含本次预期位置差异，其余页面继续存在既有字体/图标像素漂移；未在混合差异环境中更新基线。返工 Code Review 未发现阻断项，确认底部栏仅保留一套固定几何参数，状态变化仍只影响遮罩、结果卡和 Done 启用态。未修改识别、Quota、订阅、队列或后台结算逻辑。
- 2026-08-20 Scan 页面内订阅后继续识别修复：Fresh Purchase/Restore 的 typed success 只证明本机 StoreKit Premium，原 Scan 逻辑仅在已有 Waiting Item 时轮询 Quota，既不主动调用 `synchronizeServerEntitlement()`，也不会在 quota=0 且图片未入 Queue 的返回路径建立当前 session grant；后续识别因此持续收到 `ENTITLEMENT_SYNC_REQUIRED`。同时该状态的卡片标题缺少独立分支，错误落入 `No Match Found`。现已在 Quota Paywall、Scan Pro typed success 和 `ENTITLEMENT_SYNC_REQUIRED` 三个入口统一启动单任务服务端证明同步，至少刷新一次 Quota；存在 Waiting/同步卡片时继续在 15 秒窗口内确认，只有服务端返回 Unlimited 才按原图和 Queue 顺序重试。失败或超时保留同步卡片，后续扫描可开启新周期；标题改为 `Premium Syncing`，不再伪装成 No Match。修复前测试分别稳定得到“订阅成功后的同步调用次数为 0”和“同步卡片存在 `No Match Found`”，修复后 4 条定向回归测试通过；完整 Scan Widget 为 83 项行为测试通过、5 项既有 Golden 像素对比失败，未更新基线。`flutter analyze`、Dart 格式检查与 `git diff --check` 通过。Code Review 未发现阻断项，确认门禁在异常/页面销毁时释放、多卡共享同步、服务端 Unlimited 前不会重试，且 Workers 授权、免费额度预占、识别请求和后台结算契约未改。Sandbox/TestFlight 的真实 Fresh Purchase/Restore、App Attest、弱网与 session grant 时序仍需设备验收。
- 2026-08-20 Scan 小屏定位框自适应修复：原定位框固定为 `top=213px、280×400px`，没有考虑屏幕高度和底部安全区；iPhone 8 `375×667` 上框底部距离拍照区不足，Recognizing/Revealing 还使用最低 `884px` 的固定遮罩画布。现以 `390×844` 原尺寸为上限，按四边安全区、顶部完整 Free 操作区、底部拍照区及两端各 `16px` 间距计算可用区域，并保持 `280:400` 等比缩放；Free/Premium 共用顶部预留，权益切换不移动框。黄色边框、扫描线、径向暗角中心、Recognizing/Revealing 透明区和实际上传裁剪统一使用同一 Rect，拍照反馈期间视口变化时在拍照前从当前 Flutter View 读取最新几何。修复前 iPhone 8 回归稳定得到固定 `top=213px`，与期望 `168px` 相差 `45px`；修复后 iPhone 8 为约 `261.1×373px/top=168px`，紧凑 Android `360×640、safeTop=24、safeBottom=24` 为约 `222.6×318px/top=172px`，两端间距、比例、扫描线、遮罩和裁剪一致性测试均通过。完整 Scan Widget 为 86 项行为测试通过、5 项 Golden 失败（15.40%、13.73%、13.67%、6.20%、2.24%）；前三项主要包含已确认的径向暗角中心变化，Revealing 还包含前次底部栏修复，Review 保留既有字体/图标漂移，未更新混合差异基线。`flutter analyze`、Dart 格式检查和 `git diff --check` 通过。Code Review 未发现阻断项，确认正常基准尺寸不变、极小可用区域不会产生反向 clamp、视口变化不会使用旧裁剪，且未修改识别、Quota、订阅、队列或后台结算业务契约。iOS/Android 真机相机预览裁剪、旋转策略及 Sandbox/TestFlight 仍需设备验收。
- 2026-08-21 Scan 横向结果卡 loading 裁切修复：Scanning/Recognizing/Revealing 共用的 `16×16` 不定进度图标原先没有指定 `strokeAlign`，当前 Material 主题回落为居中描边，`2px` 圆弧有一半落在自身画布边界外，动画走到左侧和底部时被裁切。现仅将该图标改为 `strokeAlignInside`，保持尺寸、位置、颜色、动画和状态逻辑不变。修复前回归测试稳定得到 `strokeAlign=null`，修复后定向测试及完整 Scan Widget 中 87 项行为测试通过，`flutter analyze` 和 Dart 格式检查通过。5 项既有 Golden 仍失败（15.40%、13.73%、13.67%、6.21%、2.24%），本次只使包含 loading 的状态产生少量预期像素变化，未更新仍混有既有差异的基线。Code Review 未发现阻断项，确认未修改扫描、识别、Quota、订阅、队列或后台结算业务逻辑；iOS/Android 真机动画全周期仍需设备验收。
- 2026-08-21 Home Performance Tooltip PRD 对齐：根因是 Home 将 `market_value_change_usd` 传入图表的 Daily Change 头部，同时仅在 `market_change_usd`、`portfolio_change_usd` 非空时组装对应行，导致 Tooltip 多展示 PRD 明确禁止的 Daily Change，且无可信前序节点时只剩 Date/Qty。修复前 3 项回归分别稳定捕获多余 Daily Change、缺少 `Market: --`/`Portfolio: --` 和切换 EUR 后仍多出 Daily Change；现 Home Tooltip 固定展示 Date/Market/Portfolio/Qty，空拆分值保留行并展示 `--`，有效金额继续通过当前 CurrencyFormatter 换算和隐藏，移除已无调用者的 Home 图表 Daily Change 头部绘制通道。服务端 DTO、公式、Range、订阅和 2×2 指标均未改。Tooltip/金额隐藏/Overview 相邻路径定向 5 项及 `flutter analyze`、Dart 格式、`git diff --check` 通过；Home Widget 全文件 66 项中 63 项通过，2 项既有 Top Performers 空状态断言及 1 项既有 Performance Header Golden 10.93% 差异失败，与 Tooltip 字段路径无关。Code Review 未发现阻断项，确认语义文本与画布共用同一行数据、首点不伪造 0、币种切换和金额隐藏保持原行为；iOS/Android 真机 Tooltip 视觉仍需人工验收。
- 2026-08-21 Card Detail Performance Tooltip PRD 对齐：字段核查确认正常状态展示 Date/Daily Change/Market Value/Profit-Loss/Qty，Purchase Price 缺失状态展示 Date/Daily Change/Market Value/Qty，客户端已分别读取服务端 `profit_loss_change_usd` 与 `market_value_change_usd`，服务端公式无需修改。实际偏差是正常状态的正 Profit/Loss 使用普通金额格式，显示 `$190.00` 而不是 PRD 统一符号规则要求的 `+$190.00`；修复前回归稳定失败，现复用既有 signed amount 格式。测试夹具同时将两种 change 值拆为 40 与 55，直接保护两个状态不会取错字段。Card Detail Tooltip 定向 2 项、完整 Widget 48 项、`flutter analyze`、Dart 格式及 `git diff --check` 通过。Code Review 未发现阻断项，确认负数、零值、空值、币种换算、字段显隐、Range、订阅及服务端计算未改变；iOS/Android 真机 Tooltip 视觉仍需人工验收。
- 2026-08-31 Card Detail Performance Tooltip 文字样式对齐：按 Figma `2209:15904` 将 Performance Tooltip 每行按首个冒号拆为标签和值，`Date:`、`Daily Change:`、`Market Value:`、`Profit / Loss:`、`Qty:` 等标签统一使用 `#999578`、Geist Regular、12/18，冒号右侧日期、金额和数量统一使用品牌黄 `#F0FE6F`、10/14；行高同步扩展为 18，避免标签文字重叠。字段、数值、币种、公式、语义文本、时间范围和交互逻辑未修改，复用同一 Painter 的 Price Tooltip 保持原样。修改前样式契约测试因没有分色构造入口而编译失败；修改后样式契约与 Card Detail 完整 Widget 54/54、`flutter analyze --no-pub`、Dart 格式及限定路径 `git diff --check` 通过。Code Review 首轮发现共享 Painter 会连带修改 Price Tooltip，现已将新样式限定为 Performance 专用 `tooltipRows` 分支，复审未发现剩余阻断项；项目未内置 Geist 字体文件，Android 继续使用现有字体回退，iOS/Android 真机 Tooltip 视觉仍需人工验收。
- 2026-08-21 订阅后首次 Premium 请求 409 修复：真机网络证据确认 Apple Notification 先写入 `unlinked` purchase chain 与 transaction，随后同一 Fresh Purchase challenge proof 因“交易已存在”被错误归为 `REPLAY_REJECTED`，当前 session 没有 grant，Home Overview 1Y 与 Performance 首次请求因此返回 `ENTITLEMENT_SYNC_REQUIRED`，等 Restore proof 后才恢复。Worker 现只在 transaction/original transaction/SKU/entitlement、有效生命周期、owner 与未消费 challenge 全部匹配时原子关联 notification-first chain 并创建 session grant，保留通知订单及较新 lifecycle；并发通知在 Fresh INSERT 唯一冲突后走同一安全关联，真实跨 owner、不同 challenge、过期、撤销和退款重放仍拒绝。Flutter 在购买成功后非阻塞启动共享同步；同步先等待 Fresh queue 并查询当前 session grant，仍缺失才走 App Attest Restore。Overview 1Y 与 Performance 将精确 409 的同步及一次重试放在 Controller 内，期间保持 loading，最终失败才恢复旧 Range 或展示顶部失败提示。Workers 判定测试 21 项、Fresh/Notification 既有集成 39 项、Flutter 新增及 Home Controller 定向 34 项通过，Workers type-check 通过；Home Widget 58 项中 57 项通过，唯一失败为既有 Performance Header Figma golden 10.93% 像素差异。Workers 全量 546 项中 545 项通过，未通过项为既有 Miniflare/D1 `performance-premium.integration.test.ts`，PostgreSQL 查询在 SQLite 兼容层报 `near "ON"`，本次未修改该查询或继续维护退役 D1 基座。两轮 Code Review 在补齐 chain store/environment 与交易/chain 并发锁后未发现剩余阻断项，确认通知订单字段不被覆盖、当前 session 以外不新增 grant、重试上限为一次且 Android 无 App Store 配置保持原行为。未扩展 D1/Miniflare 测试债务，notification-first PostgreSQL 原子关联、Sandbox/TestFlight Fresh Purchase 与真机首次 1Y/Performance 仍需部署后验收。
- App PRD 验收 141-145 已完成代码与文档证据核对：各功能卡点统一引用 Functional Paywall 未成功规则，只有 typed Premium success 才恢复 `blocked_action`；PRD 前部的 `v1.1 核心规则速查` 明确仅汇总正文。服务端 Premium 授权只读取当前 `session_id` 的 Apple 购买链 grant，同 UID 另一 session 不继承；Notifications V2 与 Apple Server API 分别承担 Apple 生命周期维护和状态校正。客户端 StoreKit 2 verified Purchase/Restore 会立即把本机状态更新为 Premium，不等待通知、Admin 订单或服务端同步；Fresh Purchase 的持久同步失败直接测试仍返回 active entitlement。Flutter Subscription/Restore 14 项、`subscription-core` 9 项、Workers 权益/通知/校正/Restore 本地 Miniflare D1 兼容层集成 17 项已通过；该测试不代表运行时读取 D1。Apple 最终权威的端到端结论仍需 Sandbox/TestFlight、App Attest 真机、通知及 Server API 实网验收。
- App PRD 订阅验收 146-148 已完成代码和直接测试证据：Notifications V2 维护续订、Grace Period、Billing Retry、Billing Recovery、Expired、Refund/Revoked，Apple Server API 负责异常校正；所有授权仍绑定购买链与当前 session，UID 仅作为交易和业务关联字段。客户端读取本机 StoreKit verified entitlement，服务端或 StoreKit 暂时不可用时使用未过期 verified 缓存，缓存无法继续证明有效时进入 Unknown 而非直接降为 Free。新增本地 Miniflare D1 兼容层通知测试直接覆盖 Billing Retry 失效、Billing Recovery 恢复、Expired 失效和 Refund 撤销并更新原订单，通知集成 9 项及 Workers type-check 已通过；该测试不代表运行时读取 D1，仍需 Apple Sandbox 实际生命周期矩阵验证。原 PRD 随后对 Performance 验收再次从 146 编号，开发与测试记录分别使用“订阅 146-148”和“Performance 146-156”区分，不修改原始 PRD。
- App PRD Performance 验收 146-156 已补齐 migration 与 1Y Partial History 直接证据：`0031` 对 Existing Item 将 `performance_start_at`、`purchase_price_effective_at` 设为原 `created_at`，同时把 `performance_history_available_from` 设为 migration 时刻；既有事件保留原 `effective_at` 和原 Folder，只回填当前 Purchase Price/Currency 及可靠历史起点，因此不会把当前 Folder 或其他当前属性伪造成 v1.0 历史。1Y 仍按自然年范围返回，历史不足时从可靠起点展示真实片段并标记 `partial_history=true`，不降级、不补虚假点，也不误报 Network Error。已有数据 migration 与 Performance 计算测试 9 项、Flutter DTO/Controller 21 项及 Workers type-check 已通过；2026-08-13 又在远程 dev 只读导出的本地副本验证了 67 个 Item、145 条 Event 的迁移不变量。由于 dev 最大 owner 仅 24 条 Event、12 个现存 Item，1Y 重度用户规模和 Cloudflare 端到端性能仍需发布环境验收。
- Performance 已落地 `0031` 与独立接口：Home 按 Folder、Card Detail 严格按 `collection_item_id` 查询，统一 1D/7D/15D/1M/3M/1Y、默认 1M、15 秒超时和迟到响应隔离；服务端返回 `item_count`、`market_price_status` 与 purchase price 状态，App 可区分 Empty、No Data、Partial History 和 Missing Purchase Price。同卡多 Item 且无 Item 上下文时隐藏 Card Detail Performance，不猜测、不聚合。Home 点位已按 PRD 全精度公式分解 `market_value_change_usd`、`market_change_usd`、`portfolio_change_usd` 与 `quantity_change`；Range 首点读取范围外紧邻可靠节点，只有全历史无前态时四项返回 `null`。Folder Move 使用最新 Folder 决定全部可靠历史归属，不再把 Move 前片段留在 Source。Home Tooltip 固定展示 Date/Market/Portfolio/Qty，Daily Change 只保留为内部计算和等式校验值；Market/Portfolio 缺少可信前序值时对应行展示 `--`，有值时跟随当前 App 币种和金额隐藏状态。Partial Purchase Price 使用 Info Popover，并在点击图表、切 Range、切 Folder或离开 Performance 时执行互斥/清理。Home Performance 任一 Range 只有一个有效数据点时常驻显示强调色圆点，单点仍可点击；金额隐藏会隐藏 Market 与 Portfolio 金额且保留 Qty。Workers Portfolio 10 文件 57 项、Flutter DTO/Controller/Home 定向测试及 `flutter analyze` 已通过。`0031` 已通过隔离空库、dev 导出副本及远程 dev 实际迁移验证；常用 local/prod、App Attest/iOS 与真实历史数据规模尚未验证。
- Card Detail Performance 已按 App PRD 第 12 章和 14.8 收口：正常曲线使用 Item Profit/Loss，缺 Purchase Price 时使用 Market Value；服务端点位提供全精度计算后舍入的 `market_value_change_usd` 与 `profit_loss_change_usd`，Range 首点读取范围外紧邻可靠节点。正常 Tooltip 字段固定为 Date/Daily Change/Market Value/Profit-Loss/Qty，缺购买价状态不泄露 Profit/Loss、Purchase Cost 或 Return；任一 Range 只有一个有效数据点时常驻显示强调色圆点，单点仍可点击，切 Range 后旧 Tooltip 清空。页面指标文案同步为 Purchase Cost、Market Value、Profit/Loss、Return，正金额按 PRD 显示 `+`。Flutter Card/DTO/Controller 定向 58 项、Workers Performance 单元/授权集成 11 项及两侧静态检查通过。
- Admin 订单与通知已按通知真值收口：Fresh Purchase/Restore 的服务端验签交易仅作为即时权益和交易暂存证据，`source_notification_uuid` 为空时不进入 Admin 列表、筛选选项、XLSX 或扣款编号；已验签、已解码的建单通知到达同一 `environment + transactionId` 后用通知字段晋升暂存记录。PostgreSQL `0000`/`0007` 提供订单事实与退款前业务状态；只有已验签交易的 `price > 0` 才按 `environment + originalTransactionId`、`purchase_at + transactionId` 确定性增加扣款次数，零金额不增加，缺失金额保持未知并进入校正，重复交易不增加次数，退款保留原序号，`REFUND_REVERSED` 仅在 Server API 校正为 active 后恢复。共享 PostgreSQL Schema 已于 2026-08-19 应用 `0007` 并完成幂等复核，同日完成 dev Worker 与 Admin assets 部署；发布验收检查点 version `ce9ee177-27a0-49fe-8e87-0a0f4414b620` 已通过 health、Admin HTML/静态资源和未授权边界烟测，实时流量版本以 Cloudflare deployment 回读为准。`DID_CHANGE_RENEWAL_PREF + UPGRADE` 在当前周/年方案的新 `transactionId` 到达时新增“升级付款”订单；`DOWNGRADE` 只记录下一 Product ID，不建未来订单；空 subtype 仅补录尚未入库的真实交易。`SUBSCRIBED + RESUBSCRIBE` 使用新链时为首次付款，沿用旧链时重算为续费。已验签建单通知可创建 `unlinked` 链和订单但不创建 grant。订单页按 PRD 展示 13 列和 11 项筛选，导出当前筛选的全部通知确认订单为 XLSX。通知页使用可搜索单选主/子类型联动，补齐请求期禁用、越界回退、固定空/失败状态；详情按需加载，包含独立 Loading/失败重试、两列基本信息和深色 JSON。真实 Sandbox 通知、退款撤销、自动续订快照、升级/降级、重订阅与后续补关联仍待验收。
- 订单 USD 金额已按 Admin PRD 与评审 P1-E 补齐 `0033` 快照：现有服务返回 `1 USD = rate × 原币种`，服务端以整数 micros 执行除法换算并固化原始 rate、来源、有效/抓取时间、陈旧标记、版本和舍入模式；USD 使用恒等快照。汇率或币种不可证明时订单仍入库但 USD 保持空，历史订单不猜测回填。汇率/Fresh Purchase/Notifications 定向 28 项和 Workers type-check 已通过；`0033` 已应用到远程 dev，尚未应用到常用 local 或 prod。
- Extended Price History 已按 App PRD 统一 Home Overview 与 Card Detail Price History 六档 Range、默认 1M；Free 点击 1Y 不切 Range、不请求数据，Functional Paywall 成功后返回来源图表并自动加载 1Y，Premium 变 Free 时自动回 3M。Home 1Y `/portfolio/valuation-history?days=365` 强制当前 session grant。Card Detail 公共 `market-prices` 最多返回 90 天 history，`price-series` 单条或 batch 只在 `days<=90` 时公开；1Y Raw/Graded 均要求 live session 的服务端 grant，不允许同 UID 其他 session 继承，也不回退公开请求。本机 verified 头只用于返回同步等待态。Flutter Card Data/Card Detail 53 项、Workers 数据源/Premium helper/迁移与本地 Miniflare D1 兼容层权限 26 项及两侧类型/静态检查已通过；该测试不代表运行时读取 D1，该闭环不新增迁移。
- 配置边界：dev/test Product ID、PostgreSQL 中迁移的 dev 商品映射、Workers dev 白名单及 Sandbox 通知 URL 均已就绪；Root CA 已用官方 G3 指纹核验输入更新，Server API Secret 仍需真实调用验证，真实 Sandbox 通知仍待端到端验收。2026-08-25 已确认 production Product ID 为 `CardAi.weekly`、`CardAi.yearly`、`CardAi.lifetime`，并同步客户端配置、Workers prod 白名单及 v1.1 目标 PostgreSQL 三条 active `performance_pro` production 商品映射；`APPLE_IAP_PRIVATE_KEY` 已暂存为待部署 version `4af6f4de-eaa0-4f52-b2ad-9ac268ec7bb8`。只读远程证据确认当前 prod 仍运行 D1；production/TestFlight 双 verifier、按 Bundle 隔离的通知 inbox、双环境 grant/lifecycle/校正及 Admin scope 代码已完成，PostgreSQL `0009` 已应用并完成独立连接复核。后续顺序为 dev 新 Worker验证 → prod D1 数据迁移/冲突审计 → prod v1.1 PostgreSQL 切换 → production Sandbox URL；prod Root CA 与 Private Key 仍需切换后实网验证。
- 2026-08-25 production TestFlight Sandbox JWS 隔离修复：根因是 Workers 把 `APP_ENVIRONMENT` 一对一映射为 Apple environment，prod 只能验证 Production；即使仅增加 Sandbox verifier，production Sandbox grant 仍会被 Premium 查询拒绝。目标 v1.1 架构中 dev 与切换后的 prod 共用 PostgreSQL，验签前 inbox 只有 `environment` 时，prod cron 领取 Sandbox 会与 dev 抢队列，Admin 也会按 Sandbox 跨 App 展示通知和订单。修复前两条业务红测分别得到 production factory 实际 `Production`、期望 `Sandbox`，以及 production 对 `CardAi.weekly` Sandbox grant 实际 `false`、期望 `true`；PostgreSQL migration 测试因缺少 `0009` 文件失败。现 production Bundle 顺序构造 Production/Sandbox 两个 transaction verifier，Fresh Purchase/Restore 以 Apple 已验证 environment 归约，grant/lifecycle 还必须命中当前 Worker Product 白名单。`0009_apple_notification_app_bundle.sql` 为 inbox 增加 Bundle scope、扩展去重与 processing 索引，并用 legacy INSERT trigger 保证 dev 的“先 migration、后 Worker”窗口不打断旧入口；通知接收、重试和 Server API 校正按 Bundle + environment 隔离。Admin 通知使用同一 Bundle scope，订单/导出/选项使用 SKU + environment scope。三轮 Code Review 先否决无 Schema 半修复，随后补齐旧 Worker trigger 兼容和 Admin 下游隔离，最终无剩余代码级阻断项。定向 15 文件 134 项、Admin 16 项、Workers type-check、prod dry-run、依赖方向和 `git diff --check` 通过；Workers 全量 558 项中 556 项通过，失败为既有 Performance PostgreSQL SQL/Miniflare SQLite `near "ON"`，以及用户并发 Scan lease 改动的 `existing/exhausted` 断言，不属于本修复。`0009` 已经授权通过 owner Hyperdrive 单事务执行并独立复核；未部署 dev、未迁移/切换 prod D1、未修改 App Store Connect URL。真实 TestFlight Fresh/Restore、通知、Server API 与多设备仍待发布任务按既定顺序验收。
- 2026-08-25 后续 dev deployment（覆盖上条“未部署 dev”检查点）：提交 `38088799db8a96287c16c3ba456327e06196c657` 已部署到 `toccards-api-dev`，Cloudflare deployment 于 `2026-08-25T08:51:58.915Z` 创建，version `1c4ea3b1-ec2f-4c79-a77c-026d84292aeb` 承载 100% dev 流量。dev dry-run、Admin assets 构建、Workers type-check 与 Scan/Auth/Premium 172 项通过；线上 `/api/v1/health` 返回 `200 {"status":"ok"}`，`/admin` 返回 200 HTML，新 `POST /api/v1/scan/quota/reserve` 未授权边界返回 `401 UNAUTHORIZED`。绑定回读保持 dev Hyperdrive/KV/R2、`APP_ENVIRONMENT=development`、dev Bundle 与 `cardx.*` Product IDs；未部署 prod、未迁移/切换 prod D1、未修改 App Store Connect URL。真实 Apple Sandbox 通知、Server API、购买/Restore、多设备及真实 Scan 并发/中断仍待验收。
- 2026-08-25 dev Scan 真实烟测：临时独立游客鉴权返回 200；规范路径为 `POST /api/v1/scan/recognize`，不是省略 `/api` 的 `/v1/scan/recognize`。App 当前默认不传 `game_id` 的请求已通过 multipart 校验并进入处理，但 40 秒无任何 HTTP 响应；同一 pHash 直调 OCR 上游约 0.9 秒返回 30 个候选，证明超时发生在 Worker 收到 OCR 结果后的目录解析/审计链。带 `game_id=3` 时 OCR 返回 8 个候选，完整 Worker 请求返回 200、`recognition_status=success`、8 个候选，R2/审计/quota 结算成功（测试游客 `consumed=1`、`remaining=9`）。因此接口基础链路可达，但 Flutter 当前默认调用仍不满足 15 秒 Deadline，dev Scan 端到端验收标记为失败；需定位未过滤 30 候选的 PostgreSQL/Hyperdrive 目录解析时延，修复后用默认 App 请求复验。
- 上述 Scan 超时根因已定位为候选目录解析的 N+1 SQL：识别路由对每个 OCR 候选调用一次完整 `adapter.getCard()`；单次 `getCard()` 先查 `cards_all`，随后并发查询卡号和 published current price，共 3 次 PostgreSQL SQL。无 `game_id` 的 30 候选因此最多触发约 90 次 SQL，`game_id=3` 的 8 候选约 24 次；每个 Worker request 的 PostgreSQL client `max=5`，`Promise.all` 只能形成最多 5 条连接的分批排队。Scan 还直接创建 local DB adapter，未使用目录 KV/Cache API。现有 `findCardNumbersByProductId` 和 `loadPublishedPriceRows` 已支持 `IN (...)` 批量，且后者 chunk size 为 40；修复应把 Scan 候选归约为一次 cards、一次 number、一次 published price 批量读取，而不是提高连接数或依赖 `game_id` 绕过默认 App 路径。
- Scan N+1 最小修复已完成但尚未重新部署 dev：识别路由不再逐候选调用完整 `getCard()`，改为单条 `cards_all WHERE product_id IN (...)` 读取名称、游戏、Set、卡号和稀有度；Scan 响应不需要的 published price、语言与 finish 不再加载，Review 读取链保持不变。30 候选回归在修复前稳定记录 60 条 `cards_all` 查询并在期望 1 条处失败；修复后保持原 OCR 排名返回 30 个候选，同时为 1 条目录 SQL、0 条价格 SQL。Scan routes/quota 29 项和 Workers type-check 通过；字母数字 product ID、未解析 warning、卡号消歧、Premium/Free quota、幂等重放契约未改变。当前线上仍是修复前 dev version `1c4ea3b1-ec2f-4c79-a77c-026d84292aeb`，必须在后续明确授权的 dev deployment 后用默认无 `game_id` App 请求重新验证 15 秒 Deadline。
- Scan 批量查询修复验证：Scan/Auth/Premium 关联 4 文件 173 项通过，Workers type-check 与 `git diff --check` 通过；完整 Workers 为 560/561，通过数增加且失败集合仍只有既有 `performance-premium.integration.test.ts`，原因是 Miniflare/SQLite 不接受生产 PostgreSQL 查询中的 `ON` 语法，和 Scan 修改无关。修复后 Code Review 未发现剩余代码级阻断项；真实 dev 默认请求时延仍必须等部署后验证，不能用 Fake DB 的常数查询断言代替线上结果。
- 2026-08-26 Scan 批量查询 dev 交付：提交 `1f1d6ed6fb02f4c3b8087a5076c754102411accc` 已推送 `github/dev`，Cloudflare dev version `fbcabaef-5844-4990-b648-4c9be2f1ba26` 已承载 100% 流量。dev dry-run、Admin assets、health/Admin/未授权 reservation 烟测均通过。使用独立测试游客按 Flutter 实际两步链路、默认不传 `game_id` 复验：reserve `2.290s`、recognize `3.728s`、总计 `6.032s`，服务端 `data.elapsed=0.737s`，HTTP 200、`recognition_status=success`、quota `consumed=1/remaining=9`，满足 15 秒 Deadline；相比修复前同默认路径 `39.513s` 降低 `33.481s`（约 84.7%）。本项 dev 性能阻断已解除；真实图片准确率、并发、Worker 中断、弱网迟到响应及 Sandbox/TestFlight 仍待独立验收。
- 2026-08-26 Premium Performance PostgreSQL 测试基座收口：`performance-premium.integration.test.ts` 不再创建 Miniflare D1/SQLite，改用固定版本 `@electric-sql/pglite@0.5.7` 的内存 PostgreSQL 引擎，并通过测试专用 `D1Database` 适配器复用生产 `?` 占位符转换及 bigint/numeric/date 归一化。测试 Schema 同步使用 PostgreSQL 保留字引用和 boolean，真实执行 `performance.ts` 两处 `DISTINCT ON`，5 项 Premium/Free/session grant/1Y 权限路径全部通过；基座自身另有 2 项覆盖多 statement `exec`、类型归一化和事务 batch，完整 Workers 从 560/561 收口为 67 文件 563/563。生产 Performance SQL、运行时 PostgreSQL/Hyperdrive 和业务行为未修改；依赖方向、Workers type-check 与 `git diff --check` 通过。`pnpm peers check` 仍报告仓库既有 Vite 6/plugin-react 及 Wrangler/workers-types patch peer 不匹配，新增 PGlite 无 peer 问题。
- 2026-08-26 Collection Dashboard 5 秒误超时修复：dev 真实对照中，空账号 `/api/v1/collection/dashboard` 连续响应约 `1.4s-1.9s`，仅加入 1 张 Wishlist 卡后服务端仍全部返回 200，但连续响应约 `4.55s-5.83s`；使用 Flutter Dio 原 5 秒接收截止复测 5 次有 3 次在收到任何响应前超时。根因是 Portfolio 客户端已有 15 秒总 Deadline，但底层 `receiveTimeout` 独立固定为 5 秒，价格丰富化请求超过 5 秒时先被 Dio 中断并由 Collection 失败态显示为通用 Error。现仅将 Portfolio Dio 接收超时对齐既有 15 秒 `portfolioRequestDeadline`，连接超时继续保持 5 秒，接口、服务端查询、重试、幂等和错误语义不变。修复前配置回归明确得到期望 15 秒、实际 5 秒；修复后 Portfolio API 30 项、Collection Controller 26 项及 `flutter analyze` 通过。Controller 套件仍打印既有 SharedPreferences binding 诊断但退出码为 0；Code Review 未发现阻断项。尚未构建安装包或执行 iOS/Android 真机及修复版本 dev App 原路径验收，后端价格查询性能仍需独立分析，不以客户端放宽截止替代。
- 2026-08-26 Workers 价格查询性能修复：Cloudflare 日志中 222/773 条请求超过 1 秒，Card、Market Prices、Sold Listings、Price Series Batch、Collection Dashboard 与 Valuation History 均复用 published-current 价格查询；Canceled 请求集中在 `4.994s-5.018s` 且 CPU 仅 `1ms-8ms`，命中 Card Data 客户端 5 秒接收截止。dev 只读 `EXPLAIN ANALYZE` 证实 `series.is_active IS TRUE` 未匹配现有 `idx_price_series_card_qualifiers ... WHERE is_active` partial index，单卡价格查询改走 `uq_price_series_external_metric`，对两个来源各过滤约 411 万行，执行约 `3046ms`、shared buffer hit 约 356.9 万；将等价谓词收口为 `series.is_active` 后改用既有卡牌索引，执行约 `1.48ms`、buffer hit 217，因此不新增 PostgreSQL migration。Card/Search 同时复用主查询已读取的卡号，Card 与 Sold Listings 并发启动独立卡牌/价格读取，Valuation History 并发启动卡牌资料与价格历史读取；API 字段、筛选、排序、价格口径、鉴权、缓存 TTL 与错误语义不变。Wrangler 远程预览未部署对照中，单卡 Card 从约 `4.94s` 降至首次 `1.15s`、后续约 `0.56s`，Market Prices 为 `0.24s-0.44s`，Sold Listings 为 `0.39s-0.87s`；Data Source/Portfolio/Scan 定向 6 文件 89 项、完整 Workers 67 文件 566 项、Workers type-check、依赖方向和 `git diff --check` 均通过，修正内部查询投影类型后重跑受影响 47 项与 type-check 通过，最终 Code Review 未发现剩余代码级阻断项。Search 冷查询仍约 `2.29s`、KV 命中约 `0.24s`；尚未执行已登录 Valuation/Collection 原路径、iOS/Android 真机、dev 部署后 Cloudflare 日志复验或 prod 验证，不把远程预览当作已发布结果。
- 2026-08-26 Flutter 默认环境错配修复：未显式注入 `APP_ENV` 时，Dart 默认连接 dev API，但普通 iOS Runner 的 Bundle ID、Firebase、App Attest 及 Android 默认 application ID 均为 production，可能生成跨环境安装包。修复前环境一致性回归在期望 `defaultValue: 'production'`、实际 `test` 处稳定失败；现将未配置默认值收口为 production，test/dev 构建继续强制通过 `--flavor test --dart-define-from-file=config/test.json` 显式选择 beta 身份和 dev API，根 Melos 测试任务也显式注入 `APP_ENV=test`，并修正文档中遗漏环境参数的旧构建示例。环境/调试/发布配置定向 16 项、显式 production 环境 2 项及根 Melos analyze 均通过；完整 Melos 中 subscription-core 9 项通过，Flutter App 821 项通过、10 项跳过、13 个 Golden 像素对比失败，不能标记全绿，失败集中于 App Shell、Scan、Home、Profile/Subscription 视觉基线且未出现本次环境/API/日志行为失败。API 契约、Bundle ID、Firebase/App Attest 配置、Product ID 和 Workers 未修改。影响面复核发现直接运行无环境参数的调试日志测试会因 production 默认关闭日志而失败，标准测试入口显式注入 test 后恢复覆盖；Code Review 未发现其余阻断项。Windows 无法执行 iOS 构建与真机重装，仍需 macOS/苹果测试机按 test 命令补验。
- 2026-08-26 App 注销后 401 修复：根因是新邮箱注册或首次 OAuth 建号会把登录前匿名账号升级为正式账号，但 Flutter 注销仍无条件恢复本地 `previous_anonymous_session`；该匿名 owner 已被服务端判定失效，且未过期 JWT 在冷启动快速路径中继续被信任，导致受保护接口持续 401。修复前回归在期望验证旧匿名会话、实际未发起验证处稳定失败；现改为注销前验证历史匿名会话，有效时保留游客连续性，失效时清除旧缓存并创建新匿名会话，替代会话准备成功后才撤销用户会话，避免网络失败留下服务端已注销但本地仍显示用户的状态。Auth Controller 29 项、Repository/拦截器 23 项、注销相关 Widget 4 项及 `flutter analyze` 通过；完整 `auth_profile_test.dart` 仍有 4 个既有 Profile/Subscription Golden 像素基线失败，和本次无 UI 改动的鉴权修复无关，不能标记完整 Widget 套件通过。Code Review 未发现剩余阻断项；未构建安装包，也未执行 iOS/Android 真机的注册或首次 OAuth、注销、冷启动原路径验收。
- 2026-08-26 App 注销 401 dev 云端交付：从已推送提交 `94f755476ced52789db3e9eb56179bd3d9d7fe37` 的干净 detached worktree 安装锁定依赖，按标准 dev 脚本构建 Auth Core、Admin assets 与 Worker；Workers type-check、Admin type-check 和 dev dry-run 通过，未执行数据库 migration。Admin 测试 15 项中 14 项通过，失败项是 `billing-admin-intent.test.mjs` 仍用旧正则要求订单查询条件只有通知真值，而 2026-08-25 已提交的可执行代码还包含更严格的 SKU + Apple environment `scope.sql`，本次未把该失败标记为通过。Cloudflare deployment 于 `2026-08-26T08:16:18.000Z` 创建，version `faa23ac8-ef1c-4961-9c85-8f9dd8452a24` 当前承载 100% `toccards-api-dev` 流量；回读确认 dev Hyperdrive/KV/R2、`APP_ENVIRONMENT=development`、beta Bundle、`cardx.*` Product IDs、Secrets、custom domain 与 5 分钟 Cron 均保持。线上 `/api/v1/health` 和 `/admin` 返回 200，Admin HTML 引用的 10 个 JS/CSS 全部返回 200 且 SHA-256 与本地构建一致，未授权订单 API 返回 `401 UNAUTHORIZED`。本次未部署 prod；云端 Worker/Admin 发布不会分发 Flutter 401 修复，仍需生成并安装 test/dev App 后执行注册或首次 OAuth、注销、冷启动原路径验收。
- 2026-08-27 Android “注销账号后持续 401”二次修复与结论更正：上一轮把用户描述的“注销账号”误解为退出登录，只修复了 `POST /auth/logout`；Android 16 模拟器上 2026-08-26 08:20:59 新安装的 test/dev Debug 包仍稳定复现。App 脱敏 HTTP 证据显示删除前 Preferences、Folders、Valuation、Dashboard、Trending 均为 200，`08:32:42` 的 `DELETE /api/v1/auth/account` 返回 200，之后没有 `/auth/me` 验证或 `/auth/anonymous` 创建，`08:32:44` 起受保护接口及 Refresh 全部 401；安全存储同时存在当前 session 与 `previous_anonymous_session`。根因是正式账号删除分支仍调用旧 `_restorePreviousAnonymousOrCreate()`，无条件恢复已升级游客；上一轮修复结论只覆盖退出登录，不覆盖账号删除。现正式账号删除前先验证并准备匿名 replacement，失效历史游客会被清除并创建新游客，replacement 失败时不调用删除账号；同时只对“当前匿名 Refresh Token 与历史匿名 Refresh Token 相同”的冷启动恢复态强制服务端校验，修复已经写入坏会话的存量安装，普通未过期会话仍走快速路径。两条新增回归修复前均在期望验证历史游客、实际验证列表为空处稳定失败；修复后 Auth Controller 32 项、Repository 19 项、拦截器 4 项、删除相关 Widget 4 项和 `flutter analyze` 通过。保留模拟器数据重新构建并冷启动后，真实请求先得到 `/auth/me` 401、Refresh 401，随后 Preferences、Folders、Valuation、Dashboard、Wishlist、Search、Trending 全部恢复 200，页面不再显示 `No content available`；Code Review 未发现剩余阻断项。尚未使用新测试邮箱再次完成验证码注册、删除账号和第二次冷启动全路径，也未执行 iOS 真机验收；本次仅修改 Flutter 与测试，未重新部署 Worker/Admin、未执行数据库 migration。
- iOS 发布入口已增加分环境配置门禁：`tool/release_ios.sh` 校验环境和三个不重复的 Product ID。当前 test 与 production 的三个 Product ID 均已按环境隔离配置；Singular SDK 配置改由 Cloudflare 对应环境的公共 `/app-config` 运行时下发，不再要求本机发布 JSON。Windows 环境没有 Bash，完整 IPA 流程仍需 macOS CI/开发机执行。
- 2026-08-13 最新工程回归：Workers 50 个文件 395 项、Admin 13 项、根 TypeScript 7 个 package type-check、全仓 build、Dart/Flutter analyze、依赖方向检查、Workers dev/prod dry-run 均通过；Flutter 全量为 679 项通过、10 项跳过、1 项 App Shell Golden 失败，不能标记全绿。Workers 首轮全量曾因 Miniflare 临时端口命中 Undici `bad port` 导致 Folder 并发集成测试未执行完成；该文件单独 3 项及随后全量均通过，归类为一次性测试基础设施故障。Wrangler dry-run 因沙箱不能写用户目录日志打印 `EPERM`，但 prod/dev 打包、bindings 检查及命令退出均成功；dry-run 不证明 Apple Secret 已配置或远程环境已部署。
- iOS release 配置门禁会区分环境：`config/test.json` 保存三个 dev Product ID，`config/production.json` 保存三个 production Product ID；环境隔离测试验证两套 SKU 不交叉。Singular 配置不得进入仓库或本机 Release 配置，由 Cloudflare 分环境管理；prod Worker 必须先发布 `/app-config` 下发代码，App 才能完成 Singular 初始化。
- 历史检查点（2026-08-13，早于同日后续 dev 迁移与部署）：远程只读发布审计显示 dev/prod Worker 当时均只有既有 JWT、Mixpanel、ZeptoMail Secret，缺 Apple Root CA 和 App Store Server API Issuer/Key/Private Key；`wrangler.toml` 也尚未配置正式 Product ID 白名单、IAP Bundle ID、Production App Apple ID。当时两环境 D1 待执行 `0025` 至 `0033`，本分支新增且尚未远程执行 `0034` 后，该检查点的待执行范围为 `0025` 至 `0034`。因此当时部署即使可打包，Fresh Purchase/通知验签/Server API 校正仍会显式不可用；该次审计未写 Secret、未迁移、未部署。远程 dev 的较晚执行结果以本节首段为准。
- 2026-08-13 真实历史迁移预演：通过 Wrangler 只读导出远程 dev D1，在仓库外本地 SQLite 副本连续执行 `0025` 至 `0033`。迁移前后核心表行数一致，`quick_check=ok`、外键检查无错误；67/67 个既有 Item 的逻辑起点保持原 `created_at`，可靠历史起点统一为迁移时刻，145 条 Event 未删除或重排。单条迁移耗时 9-45 ms，`0031` 为 17 ms。当前 dev 最大 owner 只有 24 条 Event、12 个现存 Item，Performance 数据库查询本地 20 次中位数为 Event 6.71 ms、价格 7.72 ms；Admin 订单表为空，空列表/计数中位数分别为 6.52/6.41 ms。这些结果不包含 Cloudflare 网络与 Worker JS 计算，也不覆盖有订单或重度收藏用户，不能替代 App 15 秒和 Admin 普通查询 3 秒的发布环境验收。本轮未写远程 D1。

- Notifications V2 前向兼容已收口：HTTP 包络允许 `signedPayload` 之外的 Apple 新增顶层字段并在 inbox `request_json` 原样保存；未知但已验签的主通知类型与 Payload 字段进入结构化记录和 Admin 展示，在无已定义语义时不建单、不修改权益。本地 Miniflare D1 兼容层集成测试同时保护原始字段、Decoded Payload 和零业务副作用；该测试不代表运行时读取 D1。

### 阶段 A：P0 可信权益闭环

目标：关闭评审 P0-A/P0-B/P0-C，使服务端受限操作不信任 UID 或客户端布尔值。

- 鉴权上下文保留服务端验证过的 `session_id`。
- 新增 session → Apple purchase chain grant；不从 owner grant 回填。
- 新增版本化 Apple 证据同步接口，独立校验签名、Bundle、SKU、环境、有效期和撤销状态。
- Fresh Purchase 使用 session 一次性 `appAccountToken`；Restore 使用 App Attest challenge/assertion，解决合法跨设备恢复与复制 JWS 重放的冲突。
- 请求幂等、证据重放、Sandbox/Production 隔离和原始证据访问边界落库。
- Scan、Folder、Performance 与 Home Extended Price History 统一使用同一个服务端授权判断入口。
- 客户端处理 `VERIFIED_ACTIVE`、`VERIFIED_INACTIVE`、`EVIDENCE_INVALID`、`VERIFICATION_UNAVAILABLE`、`STATE_CONFLICT`、`REPLAY_REJECTED`。

验收：伪造/裸 ID 不可授权；同 UID 的另一 session 不继承；Logout 后旧 session grant 不可用；合法 Restore 可在新 session 建立独立 grant；同步失败不创建 Folder、不提交 Scan、不消耗 Free Quota。

### 阶段 B：Apple 通知与生命周期真值

当前状态：代码与 dev PostgreSQL 部署闭环，外部发布验证待完成。Notifications V2 接收、先存原文、官方验签、幂等、确定性归约、乱序保护、补偿重试、Apple Server API `correction_required` 消费与退款撤销校正已落地；dev Root CA 已用官方 G3 指纹核验输入更新并生效，接收路由、共享 PostgreSQL 表、重试任务和 Sandbox URL 已就绪，但新的真实 Sandbox 通知及完整 Sandbox/TestFlight 矩阵仍待验证。production Root CA 仅暂存未部署版本，其他配置与部署需独立完成。

- Notifications V2 接口先保存原始请求，再验签、解码和消费。
- 以 notification UUID 和 environment+transactionId 分别保证通知、交易幂等。
- 固定 purchase chain 状态版本比较元组，覆盖续订、Grace、Retry、Expired、Refund、Revoke 和 Refund Reversed。
- 不可比较或冲突时调用 App Store Server API 校正；业务消费失败可重试并可观测。
- 订单只由真实新 transactionId 创建，升级/降级通知本身不建单。
- 交易时点 USD 汇率快照代码已落地 `0033`：保存 rate、base/quote、来源、生效/抓取时间、陈旧标记、版本和舍入模式；汇率缺失不阻断订单且不伪造 USD 金额。该 D1 迁移曾在隔离空库和远程 dev 顺序执行；对应结构与 dev 数据现已迁入共享 PostgreSQL。production 实时 deployment 状态需在独立发布任务中重新核验。

验收：重复/乱序通知不重复建单或回滚新状态；验签失败仍保留原始记录且无业务副作用；校正只影响对应购买链，不按 UID 传播权益。

### 阶段 C：服务端配额与 Premium 功能

- Scan 实现服务端 Free Quota 查询、request-id 预占、逐张结算/返还、Waiting 自动递补和多设备并发。
- Folder 创建已由单条条件 INSERT 原子完成 Free 数量检查与插入，Free 最多 2 个（含默认 Folder）。
- Performance 和普通价格历史 1Y 在服务端校验 grant。

验收：客户端超时重试不重复扣额度或创建对象；Premium 同步未完成不降级消耗 Free 配额；并发请求不能突破限制。

### 阶段 D：App PRD 完整交互

- 按 PRD 实现统一 15 秒 deadline、迟到响应防覆盖和写操作幂等键。
- 已按页面实例保留最小 `blocked_action` 来源上下文；Functional Paywall 成功不进入 Success Page，非成功或目标失效不执行旧动作。
- 已实现商品局部缺失时其余 SKU 仍可购买、商品读取 1/3/8 秒自动重试共享 15 秒总 Deadline、同次 Subscribe 重新拉取，以及 Pending、Cancelled、Failed、Unverified、Purchase unavailable 和商品重载 Timeout；仍需完成前后台完整矩阵与真机验证。
- iOS 完整订阅页面与订阅弹窗已统一 SKU 的 74px 高度、22px 间距、选中/未选中 Surface、Radio、Tag、价格层级和两阶段切换过渡；Android 保持原展示，购买与 Restore 业务逻辑不变。
- 已实现 Unknown 主动刷新、Premium 变 Free、Lifetime/自动续订缓存期限和 Home/Card Detail/Folder/Scan/Profile 页面收敛；仍需 iOS 真机前后台与续订过期矩阵验证。
- 首次安装 ATT 与 Singular 初始化代码已完成，两个值已由 CF prod/dev 管理；dev `/app-config` 和 test Bundle 三套餐 Singular-only 成功事件已发布，test Weekly Fresh Purchase 已完成 iOS 真机后台入库；仍需验证 test Yearly/Lifetime、发布 prod Worker 的 `/app-config` 下发代码，并完成 production 三套餐事件、归因回传及真实 Firebase/App Store 收入验收。

验收：逐条映射 App PRD 第 21 章，不用 Widget 可见性代替服务端权限验收。

### 阶段 E：Performance 数据模型

当前状态：代码闭环，迁移与发布验证待完成。

- API 使用 PRD 固定的 `1D/7D/15D/1M/3M/1Y`，默认 `1M`，月/年按业务时区自然范围。
- 新增 `performance_start_at`、`performance_history_available_from` 和 v1.0 不可恢复历史 baseline。
- 保存 Purchase Price/Currency、Quantity、价格映射属性及 Folder Move 历史。
- 实现 Home/Card Detail 计算、缺失购买价分支、货币换算和精度规则。
- Home Performance 已实现内部 Daily Change 总涨跌、Market/Portfolio 变化分解、Tooltip 字段白名单、Qty 差值和金额隐藏；Home Tooltip 不展示 Daily Change，只展示 Date/Market/Portfolio/Qty。标题区已同步 Figma `1911:8120` 的 24px 眼睛入口。Purchase Price 数量为 0 时，标题下方显示 `Add purchase prices to track profit and return` 且不显示说明图标，图表下方不再重复该提示；数量大于 0 时显示原数量文案与 13px 说明入口。说明 Tips 按 `2209:15661` 以根 Overlay 锚定在图标上方，内容为 `Profit and return are calculated only from cards with purchase prices`，并与图表 Tooltip、Range、Folder、Tab 切换互斥清理。指标卡已按 Figma `1911:8133` 使用 100px 高的 2×2 布局、12px 间距、指定边框和纵向渐变，并复用 Card Detail 的 Purchase Cost、Current Value、Profit/Loss、Return SVG；缺 Purchase Price 时按 `1911:7793` 仅显示一张全宽 Market Value 卡，数据计算和状态分支不变。仍需真实历史数据规模与多设备远端删除人工验收。
- Home Premium Performance 已增加 `Top Performers`：服务端以当前 Folder 的独立 Collection Item 按未提前舍入的 Return 百分比降序排列，无 Return 的条目置后，返回前 5 条及总数；App 使用横向资产卡列表，标题和 `View all` 始终显示。当前 Folder 没有卡牌时，Performance 主区域复用 Home Overview 的 `Add your first card` 双操作空状态，`Scan Cards` 与 `Search Cards` 仍进入原有 Scan、Search 路由；Top Performers 没有可展示条目时复用 Most Valuable 的 `No cards in this portfolio yet` 空状态。金额隐藏只遮蔽 Profit/Loss，并在 Folder 切换时清空旧列表显示骨架。榜单条目通过统一 `home performance` 来源携带 `card_ref` 和 `collection_item_id`，Card Detail 只在该来源且 Item 有效时默认进入并加载当前 Item Performance，原有 Collection Item、Price 入口保持不变。Home Performance Range 普通失败、权益同步失败或同步后重试仍失败时均保留点击前 Range 与已有数据，并使用项目顶部失败提示；回归测试覆盖三类失败分支，以及 `View all` 进入当前 Folder 的 Portfolio 后返回时保留 Performance Tab、Range、金额隐藏和滚动位置。原始 v1.1 PRD 的“不新增其他 Performance 卡牌榜单”指 Home Overview 保持 v1.0 原流程，本模块只属于新增的 Home Performance，不扩展榜单到 Home Overview 或 Card Detail。真实设备弱网 15 秒 Timeout、Sandbox 权益同步和多设备远端数据刷新仍待人工验收。
- Home Overview、Home Performance、Card Detail Price History 与 Card Detail Performance 的六档曲线图 Range 均使用黄绿色小矩形标识当前选中项；切换后新 Range 立即获得选中背景，旧 Range 恢复未选中态，数据查询与 Premium 权限行为不变。
- 2026-08-20 曲线图 Range 选中态回归修复：根因是 Home Performance 仅切换文字颜色、没有背景装饰层，Card Detail Price History 则沿用 40×40 实心圆选中态，均与已正确实现的 Home Overview、Card Detail Performance 及当前原型不一致。修复前回归测试分别稳定得到 Home Performance 无 `Ink` 选中装饰层、Card Detail Price 选中层实际为 40×40；修复后 Home Performance 使用 24px 高、4px 圆角黄绿色渐变，Card Detail Price 保留 40×40 点击区域并把内部选中层改为 40×24、4px 圆角黄绿色渐变。影响范围仅限四个六档曲线图控件的选中态保护与上述两个缺陷控件的视觉实现，不改变 Range 查询、加载失败、Premium 1Y 或图表数据契约。Home Range/慢查询反馈相关 7 项、Card Detail Widget 全文件 44 项、`flutter analyze` 与 `git diff --check` 已通过；完整 Home Widget 文件因既有 4 项 Golden 像素差异未标记为通过，本次未更新 Golden，iOS/Android 真机视觉仍待人工验收。Code Review 未发现阻断项，确认点击区域、加载布局、Free 1Y PRO 分支及未选中态未发生行为回归。

验收：计算样例覆盖 PRD 第 10-14 章；迁移可重复且不伪造 v1.0 历史；1Y 查询达到性能门槛。

### 阶段 F：Admin、安全与发布

当前状态：订单/通知业务功能代码已完成，发布验收待完成；经最新 Admin PRD 核对，本期不新增 Payload 查看/复制审计范围。

- 订单页已按 PRD 7.4 补齐越界分页回退：刷新或数据变化导致当前页超过最新总页数时，自动请求最后一个有效页；无数据时稳定回到第 1 页，查询和重置仍按原规则回第 1 页。
- 订单请求期间查询按钮显示 Loading，并禁用查询、重置、分页和刷新入口，避免 PRD 7.2、7.5、13.1 所述重复提交；请求完成后统一恢复交互。
- 订单页已补齐 PRD 第 7、13 章的列表卡片右上角刷新/导出、页码跳转、查询输入裁边、空列表与加载失败固定文案，以及 UID、国家、安装时间、原始金额缺失时的 `--` 展示；刷新仍保留当前筛选和页码。
- 苹果通知消息页已按 PRD 第 14 章收口固定说明、筛选顺序、列表标题和缺失值 `--` 展示；主类型/子类型使用动态可搜索单选并联动，未知类型直接展示原值。列表请求期间禁用查询、重置、刷新和分页，空/失败文案固定，页码越界回退最后有效页。
- 苹果通知消息页的创建时间筛选已支持日期时间范围，刷新入口位于通知消息列表卡片右上角。
- 苹果通知详情已按 PRD 15.2 使用桌面两列、小屏单列的基本信息布局，并补齐独立加载、失败重试和深色可滚动 JSON；本地 Miniflare D1 兼容层集成测试直接覆盖通知组合筛选、失败 inbox 参与创建时间倒序分页及非法时间范围显式拒绝，避免仅凭路由存在宣称查询闭环；该测试不代表运行时读取 D1。

- 已补齐订单和通知字段、查询、通知确认扣款次数、安装时间、当前订阅状态和 XLSX 全量导出；客户端暂存交易不进入 Admin，通知到达后才晋升。本地 Miniflare D1 兼容层集成测试逐项保护订单筛选的 AND 语义、有效订单时间排序、退款事实恢复、列表/导出筛选一致性及非法筛选显式失败；该测试不代表运行时读取 D1。
- 已实现 Payload 默认安全边界：原始 `signedPayload` 长期保存但 Admin API/UI 不返回；XLSX 使用 inline string 防公式执行并限制同步导出最多 10,000 行。
- 最新 Admin PRD 未要求新增 Payload 查看/复制审计表或审计查询页面；本版本按授权详情加载、默认隐藏 `signedPayload`、用户主动复制的已确认安全契约实施，不猜测增加审计范围。
- 完成 Sandbox、TestFlight、通知失败恢复、跨设备、切号、退款撤销和服务端不可用测试。
- 验证 dev Apple Secret 内容、真实通知回调和 Sandbox/TestFlight 矩阵；production 另行补齐 Apple 密钥/根证书策略与通知 URL，并验证已配置的 Product ID 和 Singular `/app-config` 运行时链路。

验收：Admin PRD 第 20 章、App PRD 第 21 章和权益方案第 17 章全部有测试或人工验收证据；全量 CI 通过后才可进入发布。

- 2026-08-21 Search 快速收藏流程按 Figma `2363:21960`、`2363:21978`、`2363:21957` 收口。根因为旧 Search Controller 在收藏按钮点击时直接调用 Quick Collect，并把单 Item 的再次点击解释为快捷删除，无法表达“待补详情”状态。现改为账号生命周期内的本地待编辑队列：每次点击 `+` 都追加独立、默认 `Quantity=1` 的草稿，持久化 Qty 不变；待编辑提示在 Search 根页面的 Cards 与 Sets 两个 Tab 及从 Sets 进入的 Set Detail 页面显示，Home、Collection、Profile 和 Scan 隐藏，跨页时待编辑数据继续保留；单 Item 使用完整 Collection Item 编辑样式，多个 Item（包括同卡）使用顶部条及逐条/批量保存删除。每条草稿独立保留材质、评级、品相、语言、购买价和备注，保存才复用既有完整 Item Create、刷新资产并执行 Wishlist 服务端互斥，删除草稿不写服务端。灰色 `+` 对未收藏和已收藏卡牌语义一致；已有 Item 由卡片主体进入 Card Detail 管理。修复前 Controller/Widget 测试分别稳定暴露 `Qty=1` 和缺少 Pending 状态，修复后 Search Controller、Search Widget、Card Detail Widget 共 105 项通过，跨页提示与导航几何 2 项通过，`flutter analyze` 与 `git diff --check` 通过。Code Review 修正了批量保存失败后的旧下标定位、Delete All 不必要的详情加载、同卡多草稿状态隔离，并删除 Search 侧不可达的 Quick Collect/快捷删除接口；复审未发现阻断项。导航栏既有 Golden 在当前 Windows 渲染环境仍以 4.96% 文字像素差失败，未更新基线；iOS/Android 真机的弹层手势、键盘、安全区和真实弱网保存尚未验证。
- Search 待编辑提示打开的 Review/Edit 弹层固定使用视口 85% 高度并贴齐屏幕底边，顶部 15% 保留为可点击关闭的路由蒙层；底部系统安全区保留在弹层 Surface 内部，不形成可点穿的外部间隙。根因为原 `FractionallySizedBox(heightFactor: 0.94)` 使用默认居中对齐，在顶部和底部同时留空；首轮修复为 80%，后按产品确认调整为 85%，600px 测试视口在修改前稳定测得 480px，而新预期为 510px。后续横条下拉关闭审计确认，项目内通过 `showModalBottomSheet` 打开的既有横条弹层均默认启用 `enableDrag`；只有 Review/Edit 和订阅弹层使用自定义 GoRouter 页面，横条没有手势。现新增共享 `KandoBottomSheetPage`，以 Flutter `ModalBottomSheetRoute` 承载这两个路由弹层，统一获得跟手下拉、短距离回弹、越过阈值或快速下甩关闭，以及项目标准进入/退出动画；订阅弹层单独保留顶部系统安全区，并按当时产品确认将高度上限设为视口 85%，完整 Subscription Page 不受限制。600px 测试视口在修复前测得订阅弹层高度 585.33px，超过 510px 上限；修复后方案选择、购买与 Restore 继续通过弹层内部滚动访问。Review/Edit 与流畅的订阅弹层存在一处关键实现差异：85% 高度原本在包含卡图、表单和滚动区的内容树内部计算，复杂编辑器也没有独立绘制边界。现将 Review/Edit 的 85% 约束提升到路由层，并用 `RepaintBoundary` 隔离编辑器，使原生拖拽只移动已合成的内容层。第二轮定位确认中途停顿来自 `PopScope` 在路由退出动画尚未完成时同步写入待编辑草稿并重置 Riverpod 编辑状态，导致复杂表单在半途重建；现在先快照草稿，等待 `TransitionRoute.completed` 后再写入队列并清理 Controller，退出动画期间 Widget 树保持不变。加载、失败及完整编辑状态保持同一高度，普通 Card Detail Bottom Sheet 不变。修复前回归稳定捕获 Review/Edit 缺少绘制隔离及退出完成前草稿已写入；修复后 Search Widget 38 项、Card Detail Widget 49 项、Set Widget 8 项、订阅 UI 24 项、针对性 `flutter analyze`、格式检查及 `git diff --check` 通过，回归覆盖 34px 底部安全区、弹层贴底、Review 顶部蒙层关闭、短拖回弹、Review/订阅横条长拖关闭、退出期间编辑状态稳定、退出后草稿持久化、来源页状态保留、订阅 85% 高度上限及顶部安全区。Code Review 未发现阻断项；iOS Debug 包已在有线 iPhone（iOS 15.6）完成构建、安装和启动，启动期间设备网络为离线，因此未执行账户或联网流程。Review/Edit 的真实拖拽帧率与手感、iOS/Android 不同系统安全区、滚动内容手势竞争、快速下甩和键盘打开时下拉仍需人工验收。
- 2026-09-02 订阅 Bottom Sheet 高度按产品确认先由视口 85% 调整为 95%，现进一步调整为 98%；顶部安全区、横条下拉关闭、内部滚动和完整 Subscription Page 保持不变。订阅弹层路由测试同步使用 98% 上限。
- 2026-09-02 Search/Set Detail 快捷收藏 Review/Edit Bottom Sheet 高度按产品确认由视口 85% 先调整为 98%，现调整为 93%，顶部保留 7% 路由蒙层；正常入口与 `/collection-items/pending` 兼容路由保持一致。底部安全区、键盘布局、内部滚动、横条下拉关闭和退出动画不变，订阅弹层不受影响。高度调整后原慢拖测试距离未越过 98% 高度下的原生关闭阈值，现已扩大测试拖动距离以继续验证完整关闭动画；Search Widget 45/45、Set Detail Widget 8/8、`flutter analyze --no-pub`、Dart 格式与 `git diff --check` 通过。Code Review 未发现阻断项；未执行 iOS/Android 真机视觉与手势验收。
- 2026-09-02 Search/Set Detail 快捷收藏 Review/Edit 加载失败态补齐顶部拖动横条：原失败分支直接用 `_QuickCollectionFailure` 替换完整编辑 Sheet，因此显示 `Try again` 时不会构建正常态中的横条。现正常态与失败态复用同一横条组件，失败 Surface 使用顶部固定叠放，不移动居中的重试按钮；加载态、93% 高度、重试逻辑、路由蒙层和原生下拉关闭行为不变。修复前 Widget 回归稳定找到 `Try again` 但找不到横条；修复后定向回归、Search Widget 46/46、Card Detail Widget 54/54、Set Detail Widget 8/8、`flutter analyze --no-pub`、Dart 格式与 `git diff --check` 通过。Code Review 未发现阻断项，确认正常态横条几何和失败态按钮位置不变；API、Schema、收藏数据与订阅影响为 N/A，未执行 iOS/Android 真机失败态视觉和拖动手势验收。
- 2026-09-02 Search/Set Detail 快捷收藏 Review/Edit 右侧边距对齐：短收藏夹名称原位于 `Flexible` 的松约束子项中，未使用完的宽度留在标题行尾部，加上入口自身 4px 右内边距，导致 `Adding to Main` 与下方 20px 主内容右边缘明显错位；底部操作区右内边距也仍为 16px。现仅在快捷收藏 Review/Edit 中让收藏夹入口使用完整分配区域并靠右显示、移除视觉内容额外右内边距，同时将底部操作区右内边距调整为 20px；左侧标题、主内容、底部左边距、按钮尺寸、安全区和普通 Card Detail 新增收藏弹层保持不变，长名称继续单行省略。修复前几何回归在 640px 限宽弹层中测得收藏夹箭头右边缘 661.5px、目标主内容右边缘 700px；修复后收藏夹箭头和底部最右操作按钮均为 700px。定向回归、Search Widget 47/47、Card Detail Widget 54/54、Set Detail Widget 8/8、`flutter analyze --no-pub`、Dart 格式与 `git diff --check` 通过。Code Review 未发现阻断项；API、Schema、收藏数据和订阅影响为 N/A，未执行 iOS/Android 真机的短/长收藏夹名称视觉验收。
- 2026-09-02 Home Overview、Home Performance 与 Collection Portfolio 三处收藏夹切换控件改为按名称自适应宽度：短名称保持 70px 最小宽度，Overview、Performance、Collection 上限分别为 160px、120px、150px；超过可用上限时单行省略，不再缩小收藏夹名称字号，Home 保持 13px、Collection 保持 14px。

- 2026-08-26 Review/Edit 下拉关闭卡顿完成真机收口。普通 Profile 时间线与 CPU 样本确认，残余停顿并非拖拽控制器或编辑器绘制，而是正常入口把瞬时弹层作为 GoRouter 页面压入：路由移除时底层 Search Route 被重新构建，可见搜索卡片 Sliver 同帧更新；`KandoTabScaffold` 还会重复执行固定 `ColorScheme.fromSeed`，使 iPhone 11 的关闭 UI 最慢帧达到 21.8ms，其中布局内构建约 15.7ms。现由 Search 和 Set Detail 的提示条直接使用原生 `showModalBottomSheet` 打开同一 85% Review/Edit 内容，GoRouter 地址保持在来源页，原 `/collection-items/pending` 路由继续保留用于兼容；固定 Kando ColorScheme 只生成一次并复用。弹层退出完成后再持久化草稿和清理编辑 Controller，来源页提示仅监听待编辑数量，因此退出动画期间不再重建复杂表单或底层结果列表。失败回归证明没有 `/collection-items/pending` 路由时旧入口无法打开；修复后 Search 与 Set Detail 测试证明弹层可打开且路由地址不变，逐帧测试证明慢拖松手后每个 16ms 采样点继续下移。相关 Search、Set Detail、Card Detail 与 App Shell 共 103 项中 102 项通过；唯一失败为既有 Tab Bar Golden 的 5.00% 文字像素差，未更新基线。`flutter analyze`、格式检查及 `git diff --check` 通过。无额外诊断开销的 iPhone 11 Profile 同路径复测中，UI 最慢帧降至 3.9ms，Raster 最慢帧为 15.9ms，均低于 60Hz 的 16.7ms 预算；iOS 真实慢拖关闭验收完成。Android 代码继续使用 Flutter 同一原生 Bottom Sheet 路径，但 Android 真机手感、键盘打开时下拉、内容滚动手势竞争、快速下甩和不同系统安全区仍未执行人工验收。此次修复未修改服务端 API、Schema、订阅逻辑或待编辑数据契约。

- 2026-08-26 Review/Edit 卡顿修复 Code Review 复审未发现阻断项。审查确认 Search 与 Set Detail 两个正常入口均使用同一 Flutter 原生 Bottom Sheet，85% 高度、底部安全区、蒙层点击、下拉关闭、返回值成功提示及草稿退出后持久化契约保持不变；兼容 GoRouter 路由与订阅弹层不受影响。剩余风险仅为尚未执行的 Android 真机手感和键盘/滚动手势边界人工验收。

- 2026-08-26 Review/Edit 收藏夹实时同步修复：复现路径是在同一张待编辑卡首次打开时加载 `Main`，关闭后于 Collection/Home 上下文新增并选中 `Trade`，再次打开仍没有 `Adding to Trade`；删除 `Trade` 后旧草稿还可能继续显示该名称。根因是按卡的 `quickCollectionCardDetailControllerProvider` 长期缓存首次加载的 `portfolioFolders`，Folder 创建、重命名、设默认、排序和删除成功后只失效完整 Card Detail，未失效 Review/Edit 的共享 Folder 真源；弹层恢复草稿又会覆盖 `selectedPortfolioFolderProvider` 的 Home 当前选择。现所有成功 Folder Mutation 同步失效 `collectionEditorFoldersProvider`，Review/Edit 每张待编辑 Item 在本次弹层首次激活时把最新 Folder 列表同步进轻量 Controller，并以共享当前 Folder 初始化左上角选择；数量、评级、品相、语言、材质、购买价和备注仍从草稿恢复，同一次弹层内切换卡片仍保留用户主动选择的 Folder，失败 Mutation 和完整 Card Detail 加载行为不变。修复前 Widget 回归稳定得到期望 `Adding to Trade`、实际 0 个匹配；修复后新增/删除/重开回归、五类 Folder Mutation 失效回归、Search 40 项、Collection 27 项、Card Detail Controller 44 项、Card Detail/Set Widget 58 项、`flutter analyze`、Dart 格式及 `git diff --check` 通过。Code Review 未发现阻断项，确认未修改 API、Schema、服务端授权、待编辑队列身份或弹层动画；未执行 iOS/Android 真机的跨页新建/删除与弱网刷新人工验收。

- 2026-08-26 Add Portfolio 保存按钮补齐执行中反馈：原创建流程已有 `_isSaving` 去重，能够禁用输入、返回和重复 Save，但按钮仅将 `SAVE` 改为 `SAVING`，没有设计系统要求的 loading 指示。修复前悬挂创建请求回归稳定找到 `SAVING`、但按钮内找不到 `CircularProgressIndicator`；现保持 56px 胶囊尺寸和 `SAVING` 文案，在按钮内增加 18px 深色进度环，继续使用同一 `_isSaving` 禁用点击，失败后恢复 `SAVE` 和输入。Collection Widget 32 项、Home 收藏夹入口定向用例、`flutter analyze`、Dart 格式及 `git diff --check` 通过。Code Review 未发现阻断项；未修改 Folder API、权益限制、错误提示或弹层导航，未执行 iOS/Android 真机动画与弱网视觉验收。

- 2026-08-24 `+` 多次点击语义最终收口：无论 `Qty=0` 或 `Qty>0`，每次点击都新增一个独立待编辑 Item，不累加同一草稿 Quantity。每条使用独立本地 ID 和持久化表单草稿；同卡多个条目在 Review 顶部独立切换，关闭/重开不丢失各自字段，删除当前只移除一条，`ADD ALL CARDS` 逐条执行完整 Item Create。前一版把已收藏灰色 `+` 解释为打开已有 Item 管理，之后又只对已收藏卡牌拆分条目，均与最终产品口径冲突，相关未提交路由、弹层和测试已撤销。修复前回归稳定把两次点击合并为一个 `Quantity=2` 草稿；修复后 Search Controller、Search Widget、Card Detail Widget 共 107 项、`flutter analyze` 与 `git diff --check` 通过，测试证明同卡两次点击得到两个 `Quantity=1` Item，可分别保留 Quantity 3/4 草稿并保存为 Raw 与 BGS。Code Review 修正了切换/退出时同一帧读取旧草稿、批量保存失败后重建丢失错误提示的问题，复审未发现阻断项。卡片主体继续负责打开 Card Detail 管理已有 Item，Wishlist 在已收藏和待编辑状态均隐藏。未修改服务端 API、Schema、Workers、重复 Item 身份或 Wishlist 快捷逻辑。

- 2026-08-24 收藏待编辑与通用详情按新 PRD、Figma `2387:26055`、`2363:23442`、`2363:23793` 收口：Search 等无 `collection_item_id` 的入口固定进入通用详情，当前 Folder 的正式 Item 通过 `In Your Portfolio` 逐条展示并可打开编辑 Sheet；具体 Item 入口继续保留原 Performance。Search `Qty` 改为汇总全部 Folder 的正式 Quantity，而通用详情仍只读取当前 Folder。待编辑队列增加全局 20 条上限，批量保存继续处理后续条目并区分全成功、部分成功和全失败；成功 Toast 按 `260×204`、56px 图标和 20/26 标题实现。Code Review 发现原创建链路按草稿内容复用幂等键，可能在两条相同草稿遇到超时或迟到响应时串单；修复前新增 API 回归因缺少显式键参数失败，现每条待编辑 Item 使用独立 UUID 并贯穿 Review、Controller、Repository 和 API `Idempotency-Key`，同一条重试复用、不同条互不复用。受影响 API/Controller/Search/Card Detail/Toast 共 189 项测试及 `flutter analyze` 通过；Code Review 复审未发现阻断项。后续产品确认进一步区分详情页右上动作：Collection、Most Valuable、Top Performers 必须携带具体 `collection_item_id`，进入后不展示 `In Your Portfolio`，右上角使用分享图标并调用既有分享业务；Search、Wishlist、Trending 等通用入口保留 `In Your Portfolio`，右上角使用收藏图标并直接打开新增 Collection Item 编辑 Sheet，不静默写入待编辑队列。该确认覆盖原始只读 PRD 中“通用详情收藏按钮先建立待编辑 Item”的描述。未修改 Workers 路由、数据库 Schema、市场价格公式或 Wishlist 快捷逻辑；iOS/Android 真机的弹层手势、键盘、安全区、居中 Toast 模糊效果和真实弱网迟到响应仍需人工验收。

- 2026-08-24 具体 Item 详情入口与右上动作整改：根因是 Collection 卡片模型虽持有 Item ID，但共享卡片跳转未把它写入 URL；Most Valuable 又在服务端 DTO 转首页模型时丢失 `item_id`；通用详情右上收藏按钮则被上一版改为只追加本地 Pending Item。修复前三项回归分别稳定捕获 Collection URL 缺少 `item-charizard`、Most Valuable 映射缺少 `item-1/item-3/item-2`，以及通用详情点击后 Pending 队列非空且未打开编辑 Sheet。现 Collection 仅在 `entry=edit` 时携带自身 `item_id`，Most Valuable 保留并携带服务端 Item ID，Top Performers 保持既有明确 Item ID；有效具体 Item 详情严格不渲染 `In Your Portfolio`，右上固定复用 `assets/collection/share.svg`、分享 Analytics 与系统分享业务。其他通用入口不猜测 Item，已有持仓时继续显示当前 Folder 的 `In Your Portfolio`，右上 `assets/search/collection_off.svg` 直接打开新增 Collection Item Sheet，关闭未保存不改变正式 Qty 或 Pending 队列；Search 列表自身的多次 `+` 待编辑队列保持不变。修复后 Collection/Search 完整相关文件 59/59、Card Detail/分享动作 50/50、三项原始失败复现、Most Valuable 与 Top Performers 路由定向测试、Dart 格式、`flutter analyze` 和 `git diff --check` 通过。Home 扩展运行中新增 Most Valuable 路由通过，另有 7 项与本路径无关的既有结果未通过：4 项 Golden 像素差异、2 项 Top Performers 空状态断言和 1 项 Most Valuable 视觉 finder 多匹配；未更新基线或放宽断言。Code Review 发现具体 Item 分享分支仍额外依赖聚合 `detail.isCollected`，已改为严格按通用/具体入口二选一并重新验证，复审无剩余代码级发现。未执行 iOS/Android 真机分享 Sheet、弹层手势和安全区人工验收。

- 2026-08-24 `In Your Portfolio` 编辑 Sheet 弱网打开时序修复：根因是点击正式 Item 后先等待按该 Item 的 Language/Finish 刷新市场价，刷新期间页面把价格显示为 `--`，请求完成后才展示编辑 Sheet。修复前使用悬挂市场价响应的 390×844 Widget 回归稳定证明请求已发出但 `SAVE CHANGES` 尚未出现；现保持原价格刷新、规格口径和失败处理不变，仅让 Sheet 在草稿同步建立后立即打开，刷新在后台继续。Card Detail Widget 与 Controller 90/90、`flutter analyze`、Dart 格式和 `git diff --check` 通过；Code Review 未发现阻断项。影响仅限通用 Card Detail 的 `In Your Portfolio` 编辑入口，未修改具体 Item 详情、保存逻辑、API、Schema、价格公式或其他平台分支；iOS/Android 真机弱网下的弹层动画、手势、键盘和安全区仍待人工验收。

- 2026-08-24 `In Your Portfolio` 编辑 Sheet 增加移除动作：按 Figma `2363:24307` 与 `2642:26500`，仅在通用 Card Detail 的正式 Item 编辑 Sheet 表单底部复用既有黄色 `Remove from Portfolio` 按钮；点击后复用 Figma `2642:26857` 确认 Sheet，文案为 `This card will be removed from your portfolio`，取消保持正式 Item 与编辑草稿，确认成功后删除对应 `collection_item_id` 并依次关闭确认层和编辑层。修复前 390×844 Widget 回归稳定找不到移除按钮；修复后同时保护取消、确认删除、双层退场，以及具体 Item 内联编辑不出现新增按钮。Card Detail Widget 与 Controller 91/91、`flutter analyze`、Dart 格式和 `git diff --check` 通过；Code Review 未发现阻断项。未修改新增 Collection Item、Pending Review、具体 Item 编辑、Wishlist、API、Schema 或价格逻辑；iOS/Android 真机的长表单滚动、双层 Sheet 动画、手势和安全区仍待人工验收。

- 2026-08-24 单张 `Add this card` 错误反馈改为顶部 Toast：根因是 Card Detail 新增 Sheet、Search/Sets 单条 Pending Review 和 Scan Review 的底部固定提交按钮，与表单末尾行内错误不在同一可视区域；本地校验或重复 Item 失败后虽然草稿保留，但未滚到底部就看不到原因。修复前 Card Detail 重复 Item、Pending Quantity=0、Scan Quantity=0 三条回归均稳定产生行内错误但找不到 `kando-top-toast`；现两处实际按钮处理器统一在失败时使用项目顶部 failure Toast，展示具体校验/业务错误，未知异常使用通用失败文案，同时保留原行内错误和草稿。批量 `ADD ALL CARDS`、已有 Item 的 `SAVE CHANGES`、成功反馈、API、Schema 和校验规则未修改。三条原始回归通过，Card Detail/Search Widget 81/81、Scan 行为 87/87、`flutter analyze`、Dart 格式和 `git diff --check` 通过；Scan 完整套件仍有 4 个既有 390×844 Golden 像素基线失败，失败集合未扩大且未更新基线。Code Review 未发现阻断项；iOS/Android 真机顶部 Toast 与键盘、长表单及安全区叠加仍待人工验收。

- 2026-08-24 Search/Sets Pending Review 的 `ADD ALL CARDS` 批次反馈原子化：根因是顺序创建循环每成功一条就立即从本地 Pending 队列删除，导致批量请求尚未结束时顶部卡片逐张消失。修复前受控延迟回归在第一条成功、第二条仍 pending 时稳定观察到队列由 2 提前变为 1；现先收集成功 Item ID，等待整批请求全部结算后再同步更新本地队列。全部成功时顶部列表在处理期间保持完整，结算后退出 Review 并显示现有居中成功提示；部分成功时一次性移除成功项，只保留失败项，并显示 `N card(s) added, M failed.` 顶部 warning；全部失败继续保留全部 Item 并显示 failure Toast。每条请求仍顺序执行并复用自身 Idempotency Key，API、Schema、单条保存、Scan 已有原子批量流程和成功数据口径未修改。延迟批次与部分成功原始回归通过，Search/Sets/Card Detail Widget 90/90、`flutter analyze`、Dart 格式和 `git diff --check` 通过；Code Review 未发现阻断项。未执行 iOS/Android 真机弱网批量动画与 Toast 安全区验收。

- 2026-08-24 Search/Sets Pending Review 卡牌切换加载优化：根因一是 Review 只在选中卡牌后才创建对应 Card Detail Provider，首次切换到每个不同 `card_id` 都是冷加载；根因二是每次切换先清空 `_activeItemId`，即使目标数据已缓存也会强制进入整页 Loading；根因三是 Review 复用完整 Card Detail 链路，按卡重复加载 Folder、Collection Items、Wishlist、Price Series 和 Sold Listings。现新增与正常 Card Detail 隔离的 Review 轻量 Provider，真实 sectioned 仓储仅加载 Card Core、Market Prices，并通过账号级共享 Future 只加载一次 Folder；不请求 Collection Items、Wishlist、Price Series 或 Sold Listings。进入 Review 及后续切换时按 `card_id` 去重预取当前卡之后最多两张，并非阻断预缓存缩略图；目标 Provider 已具备 Folder 时同步恢复 Pending 草稿，同一帧保持编辑 Sheet，不再显示整页 Loading。保存后失效正常 Card Detail Provider，避免随后进入详情读取旧缓存；批量保存等待轻量数据完整就绪后仍复用每条原 Idempotency Key。修复前请求计数回归仅观察到首张 `squirtle` Core 加载；修复后 `squirtle` 与 `charizard-ex` 均预取 Core/Market，Folder 1 次，完整 Asset/Price Series/Sold Listings 均为 0 次，点击第二张后一帧编辑 Sheet 仍存在。Search、Sets、Card Detail Controller/Widget 134/134、`flutter analyze`、Dart 格式和 `git diff --check` 通过；Code Review 未发现阻断项。自动化未声明真实耗时数字，iOS/Android 真机冷启动、弱网切换、图片解码和 20 条队列内存/请求峰值仍待人工验收。

- 2026-08-25 Search/Sets Pending Review 批量保存时序与返回上下文修复：用户实测 6 张 Magic 卡点击 `ADD ALL CARDS` 后出现闪屏、约 10 秒无进度等待，并在成功返回 Search 后回到默认 Game。根因一是每条创建成功都会清空所观察 Controller 的 Draft，Review 随后进入 `_QuickCollectionLoading` 并重建当前表单；根因二是不同卡牌仍严格串行等待轻量数据和 Create 请求；根因三是每条成功都分别失效 Search Controller，重建时没有旧状态可传入，因而回退目录默认 Game。现批量开始时冻结当前编辑 `CardDetailState` 作为只读显示快照，底部持续展示 `Saving X of N`，后台 Draft 清理不再造成 Sheet 消失或闪屏；待保存 Item 按 `card_id` 分组，同卡保持串行，不同卡最多 3 组并发，每条继续使用自身原 Idempotency Key。批量中的单条保存不再刷新全局消费者，整批结束后按成功 `card_id` 去重失效完整 Card Detail，并只刷新一次 Home、Performance、Collection 与 Search；Search 使用 `refreshPreservingContent()` 保留当前 Game、Tab、搜索词与页面内容。全部成功仍在返回原页面后显示居中数量 Toast，部分成功仍一次性移除成功项并只保留失败项。修复前三卡受控请求回归仅启动 1 条，非默认 One Piece 返回后实际为 Pokemon；修复后 3 条不同卡请求同时启动、进度从 `Saving 0 of 3` 更新为 `Saving 1 of 3` 且编辑 Sheet 始终存在，返回后 One Piece 保持并确认 Search 只加载一次。Search、Sets、Card Detail Controller/Widget 136/136、`flutter analyze`、Dart 格式和 `git diff --check` 通过；Code Review 未发现阻断项。自动化未声明 6 张卡真实耗时，iOS/Android 真机 Magic 6 张弱网时延、并发峰值、路由动画与 Toast 安全区仍待人工验收。

- 2026-08-24 共享卡牌列表价格与涨幅统一：产品确认 Collection 卡片保留 `单价 × Quantity`，问题是 Quantity=1 时同规格卡牌在 Home Most Valuable、Search Cards、Sets、Wishlist、Collection、Home Trending 与 Trending Today 选到不同 series 或不同涨幅窗口。根因一是 Search 在同规格多价格源中按规范 SKU、30D 涨幅和数据新鲜度选择 canonical row，而 Portfolio/Wishlist 仍按较早 series 或数据库顺序；根因二是 Trending 把 1D 排名涨幅直接用于卡片展示。现 Workers 共享同一 canonical 比较器，Collection/Home 当前卡片与 Wishlist 使用同一规格选择；迁移 `price_series_id` 只保留在总资产、Performance 和历史回放。Trending 继续按 1D 快照排名，但 winning series 的展示价格读取当前发布快照并返回 30D 基准/涨幅，Flutter 统一显示 30D；当前 winning series 已失效时显式缺价，不替换规格或回退旧快照。Collection 的 Quantity 乘法保持，长涨幅文本固定单行，不能挤压 186px 卡图。修复前回归分别稳定得到 Collection/Home/Wishlist `$20 / +12.34%` 对 Search `$30 / +100%`、Trending 缺少 30D 字段、Home Trending 错显 `+25%` 而不是 `+7.5%`，以及迁移绑定价格泄漏到当前列表；修复后 Workers 目录/路由/缓存/Collection/Most Valuable/Performance 91 项、Flutter Search/Home/Sets/Wishlist/Collection 92 项及 Trending Today Widget 定向测试通过，Workers type-check、`flutter analyze`、根依赖方向和 `git diff --check` 通过。Code Review 修正了失效 winning series 不应替换规格或回退旧快照、迁移 series 当前展示与历史估值边界，以及长涨幅换行挤压卡图问题，复审无剩余阻断项。未修改 Schema、导入计算、Trending 排名、Collection Quantity、Portfolio 总资产或 Performance 历史；iOS/Android 真机仅剩视觉与真实价格抽样验收。

- 2026-08-24 Sets 卡牌列表快速收藏同步整改：Sets 虽复用 SearchCardTile 和 SearchController，但此前只具备隐式单击入队，未完整保护 Search Cards 已确认的重复点击、20 条上限、Game 上下文、资产刷新和反馈契约。失败回归证实 Magic Set 创建的 Pending Item 错用全局 Pokemon 选择，Sets 下拉刷新后资产请求次数仍为 1、Qty/Wishlist 保持旧状态，点击 `+` 后当前 Sets 页面没有 `x cards waiting for details`，而批量保存最后一条触发空队列自动返回和 `_saveAll()` 带结果返回的双重 pop，可能把 Sets 一并弹掉并吞掉成功反馈。现共享卡片动作允许 Sets 显式传入当前 Set Game；Sets 下拉刷新并行刷新当前 Set 卡牌和 Search 资产快照；共享 PendingNotice 在 Sets 安全区底部以浮层显示并为 Grid 保留滚动空间。无论未收藏或已收藏，同卡重复 `+` 继续生成独立 Quantity=1 Pending Item，正式 Qty 保存前不变，黄色 Pending 状态隐藏 Wishlist，第 21 条继续使用统一顶部 warning Toast。单条或 `ADD ALL CARDS` 全部成功时 Review 返回成功数量，原 Search/Sets 页面在路由关闭后显示居中 Success Toast；部分失败继续停留 Review 并保留顶部 warning。Sets Widget 8 项及 Search/Card Detail/Toast 共享影响面 126 项、`flutter analyze` 与 `git diff --check` 通过。Code Review 修正批量成功退场期间 `_isSavingAll` 过早复位造成的第二次 pop，复审无阻断项。未修改 API、Schema、Review 创建请求、Search Cards 业务或 Wishlist 服务端互斥；iOS/Android 真机浮层、安全区和返回反馈仍待人工验收。

- 2026-08-24 Home Trending / Trending Today 1D 口径回退：产品复核确认上条“共享卡牌列表价格与涨幅统一”把 Trending 页面展示改为 30D 属于统一其他列表时误带，原始 1D / 24h 业务逻辑继续有效并覆盖上条记录中的 Trending 30D 结论。修复前既有 Home Repository 回归稳定得到期望 `+1.25%`、实际 `+30.75%`，且只有有效 1D、没有 30D 的三张卡被全部过滤；Home 刷新和 Trending Today Widget 回归分别得到期望 25、实际 7.5，以及期望 `+5.00%`、实际显示 30D。现 Workers `/cards/trending` 恢复直接映射已发布 `card_trending_snapshot` winner 的当前价、1D 基准和 1D 涨幅，不再为 Trending 二次查询 current price；Flutter Home 预览和 Trending Today 完整列表恢复显示 `priceChange1dPercent`，不再要求 30D 存在。Search、Sets、Wishlist、Collection、Most Valuable 的 canonical current price 与 30D 统一、Card Detail 7D、Collection Quantity、Schema、导入和 Trending 排名均未改变。Workers 相关 56 项、Flutter Home Repository / Controller / Search Controller 66 项及 Trending Widget 6 项通过；Workers type-check、`flutter analyze`、Dart 格式、依赖方向和 `git diff --check` 通过。扩展 Widget 95 项通过，仍有 4 个既有 Home Golden、2 个既有 Top Performers 空状态和 1 个既有 Most Valuable finder 失败，失败集合未扩大。Code Review 未发现阻断项；未执行 dev 部署、真实价格抽样和 iOS / Android 真机视觉验收。

- 2026-08-24 Top Performers 空状态文案修复：根因是实现显示语法不正确的 `No cards in this top performers yet`，与 v1.1 已确认的“复用 Most Valuable 空状态”契约及既有两条回归不一致。仅将 `home-top-performers-empty` 文案恢复为 `No cards in this portfolio yet`，未改变整个 Performance 为空与仅榜单为空两条分支的容器、尺寸、导航或数据条件。两条原失败回归分别通过；完整 Home Widget 62 项通过，失败从 7 项降为 5 项，只剩 4 个既有 Golden 和 1 个既有 Most Valuable finder。`flutter analyze` 与 `git diff --check` 通过；Dart formatter 检查发现 `home_page.dart` 存在整文件既有格式差异，为避免无关格式化未执行写入。Code Review 未发现阻断项；未执行 iOS / Android 真机视觉验收。

- 2026-08-24 Most Valuable 涨幅徽标测试 finder 修复：生产 UI 的徽标仍正确使用 `Positioned(top: 0, right: -2)`，失败根因是测试从 `+3.20%` 向上匹配所有 `Positioned`，同时命中徽标自身和 App Shell 的 `Positioned.fill` 后被 `tester.widget()` 的唯一性要求拒绝。测试现限定在第一张 Most Valuable 卡片内部，并要求恰好存在一个 `top=0`、`right=-2` 的 `Positioned`，玻璃背景、颜色和文字样式断言保持不变。定向回归通过；完整 Home Widget 63 项通过，只剩 4 个既有 Golden。`flutter analyze` 与 `git diff --check` 通过；Dart formatter 检查发现 `home_page_test.dart` 存在整文件既有格式差异，为避免无关格式化未执行写入。Code Review 未发现阻断项；生产代码、业务行为和 Golden 基线均未修改。

- 2026-08-26 Card Detail Price 的 `RAW / GRADED` 切换控件按 Figma `2363:24493` 更新为原位置的 `150×24` 双段连体样式：6px 外圆角、细边框和轻阴影，选中段使用黄绿色纵向渐变及 Accent 文字，未选中段使用 Surface 背景和次级文字。仅修改控件视觉，原父级位置、20px 图表间距、点击切换、数据系列、Range、Market Prices 和订阅权限逻辑保持不变。修复前新增视觉回归因找不到新控件 Key 稳定失败；修复后 Card Detail Widget 49 项、Card Detail Controller 44 项、`flutter analyze`、Dart 格式及 `git diff --check` 通过，测试覆盖控件尺寸、初始选中态和点击后的双段样式互换。Code Review 未发现阻断项；未执行 iOS/Android 真机视觉与触控验收。

- 2026-08-26 Home/Collection 共用的 Portfolio 文件夹编辑与删除等待反馈修复：根因是 Edit Sheet 先返回名称并立即关闭、外层才等待 Rename，Delete 确认层也先返回 `true` 并立即关闭、外层才等待 Delete，导致慢请求没有执行中反馈且失败时原操作上下文已消失。现两个异步 Mutation 都由各自 Sheet 持有：Edit 保存期间显示 18px loading 与大写 `SAVING`，Delete 按钮显示 loading；请求期间禁用输入、取消、重复提交、返回和下拉关闭，成功后才关闭当前操作 Sheet，失败则保留 Sheet、恢复按钮并显示项目顶部 failure Toast。文件夹管理层、Home 当前 Portfolio、Collection 列表、API、Controller 返回契约和 Analytics 事件不变。修复前可控延迟回归分别稳定找不到 `SAVING` 和 Delete loading；修复后成功与删除失败三条原始回归、Collection Widget 34 项、Home Rename 定向回归、`flutter analyze`、Dart 格式及 `git diff --check` 通过。Code Review 未发现阻断项；未执行 iOS/Android 真机弱网、返回手势和 loading 动画视觉验收。

- 2026-08-26 Review/Edit 多卡状态的 `DELETE ALL CARDS` 窄屏换行修复：根因是底部两个批量按钮等分可用宽度后，Delete 按钮仍继承 OutlinedButton 默认的较大水平内边距，导致部分窄屏机型文字空间不足。现仅将该按钮左右内边距缩为 8px，并固定单行；极窄宽度或系统字体放大时使用 `scaleDown` 保留完整文案，不裁切、不换行。按钮原宽度、位置、点击区域、`ADD ALL CARDS`、批量删除回调和 Review/Edit 弹层布局均不变。修复前 `320×700` 回归稳定测得文字高度 60px、实际拆成三行；修复后恢复为单行，Search/Card Detail Widget 90 项、`flutter analyze`、Dart 格式及 `git diff --check` 通过。Code Review 未发现阻断项；未执行 iOS/Android 真机的系统字体缩放与极窄屏视觉验收。
- 2026-08-27 Admin 安装统计明细日期排序修复：根因是分页明细 SQL 使用 `ORDER BY MIN(first_seen_at) ASC`，导致运营首先看到最早安装记录；趋势查询的日期正序是图表时间轴契约，不属于问题。修复前回归稳定得到 `07-07/07-08/07-09`，现仅将明细主排序改为响应日期倒序，并继续以 UID、国家和平台升序作为同日稳定排序，筛选、聚合、趋势、分页大小和前端字段不变。原始复现及 Admin routes 19/19、Workers 全量 67 文件 567/567、Workers type-check、依赖方向、dev dry-run 和 `git diff --check` 通过；首次并行全量运行曾有 1 项 Undici `bad port` 失败，该项隔离 4/4 及无并行构建的第二次全量均通过。Admin Web 全量 17 项中 16 项通过，既有 `billing-admin-intent` 源码正则断言失败，与安装排序路径无关，未放宽或跳过。Code Review 首轮发现完整时间戳会抢在同日稳定键之前排序，已改为按页面实际展示的日期列排序；复审无剩余阻断项。未执行 dev 部署或真实 Admin 会话验收。

- 2026-08-27 Scan 订阅后顶部额度提示修复：根因是页面已经通过本地 Premium 或服务端 `unlimited=true` 放开扫描额度，却又把同一状态映射为独立的 `Unlimited scans` 提示卡，导致购买成功返回 Scan 后仍显示额度区域。现 Premium/Unlimited 状态完全隐藏顶部额度提示，仅 Free 状态继续显示 Pro 与剩余次数卡；扫描权限、额度刷新、购买返回和等待任务恢复逻辑不变。修复前定向回归稳定找到 `scan-unlimited-pill`，修复后本地 Premium、服务端 Unlimited、Free 初始额度与 Free 扫描后额度四条回归通过，`flutter analyze`、Dart 格式和 `git diff --check` 通过。完整 Scan Widget 94 项中 89 项通过，5 项仍为与本分支无关的既有 Golden 像素基线差异，未更新基线；Code Review 未发现阻断项。连接中的 iOS 15.6 真机已热重载，当前 Scan Widget 树确认不再包含 `Unlimited scans`、`scan-unlimited-pill` 或 Free 次数文本；Android 真机未执行。

- 2026-08-31 v1.0.1 Mixpanel/Firebase 新增埋点：按《埋点文档1.1》黄色区域新增 `homePerformance_view`、`subscribe_view`、`sub_click`、`sub_success`、`sub_result`、`restore_result`，并继续为自定义事件同步公共属性 `sub_plan`；六个事件同时发送 Mixpanel 与 Firebase，Firebase 参数按平台约束映射为小写安全名称。Subscription Page/Paywall 曝光携带表格原始大小写的 `Scene`；引导、冷启动、顶部入口、Profile Banner、Home/Card Detail Performance、1Y Range、Scan Tip/次数耗尽/Waiting 均映射到对应枚举，表格未定义独立枚举的 Folder 上限等普通入口归入 `Usual`。Subscribe 点击使用当前 App Store 商品的 SKU、原始数值价格和币种快照；异步成功、取消、失败沿用该快照，`sub_success` 仅在可信 Apple Purchase 且 JWS 的 Product ID、原始交易 ID、价格和币种完整匹配时上报。Restore 仅在明确 Success、Not Found、Failed 时上报，Apple 账号取消不伪报 Failed；Firebase 标准 Purchase 收入事件及去重链路不变。修复前事件集合与引导曝光回归稳定失败；修复后事件/属性精确拼写、Scene 映射、可信 JWS、曝光/点击、双通道发送和 Home Performance 切换去重定向回归通过，`flutter analyze` 与 `git diff --check` 通过。API、Schema、服务端部署和用户操作方式影响为 N/A；iOS Sandbox/TestFlight 的真实 Mixpanel/Firebase 到达与购买/恢复全链路仍需外部账号环境补验。

- 2026-09-01 Mixpanel 冷启动初始化重试修复：根因是 App 在 `runApp` 前仅执行一次 Project Token 加载与 `Mixpanel.init`，任一步骤失败后本次进程中的 Mixpanel 客户端永久为空，后续订阅漏斗等事件均无法发送，同时网络配置请求会阻塞启动。现每次完整冷启动同步创建 Analytics 容器并异步发起一次初始化，失败后仅在当前进程内按 2 秒、5 秒、15 秒重试三次；成功后先同步当前登录身份，再按原顺序补发初始化期间暂存在内存中的事件，四次尝试均失败则清空队列且不再重试。Firebase、自定义事件属性、点击防抖、前后台切换和业务流程不变。修复前重试契约回归因不存在重试实现稳定失败；修复后重试次数/间隔、首次成功、最终停止、Token 加载及相关 Analytics 回归通过。API、Schema、服务端部署和用户操作方式影响为 N/A；真实 dev/prod Token、弱网冷启动及 Mixpanel 后台到达仍需真机环境补验。

- 2026-09-01 自定义埋点 `uid` 冷启动填充修复：埋点表把 `uid` 定义为任意自定义事件的公共参数，但 App 首帧在认证会话仍为 loading 时先以空身份调用 Analytics，导致 `splash_view` 等启动期事件已经用空 `uid` 组装；Mixpanel SDK 持有上次登录的 Distinct ID 不会回填该自定义属性。现 Analytics 在首次认证结果未决时暂存全部自定义事件，`KandoApp` 不再把 loading 当成无身份；恢复账号后以用户 UID、恢复游客后以匿名 ID 统一组装并向 Mixpanel/Firebase 发送，认证明确失败且无可用身份时才按表格契约发送空值。后续登录、退出和已经组装事件的身份归属、Distinct ID 规则、Firebase 标准 Purchase、订阅权益及业务流程不变。修复前多个启动期事件与游客事件回归稳定得到空 `uid`；修复后 46 个自定义事件公共 UID、账号/游客/无身份边界、Mixpanel 初始化先后顺序和真实 App 冷启动接线回归通过，相关埋点/认证/启动定向测试通过，`flutter analyze --no-pub` 与 `git diff --check` 通过。根 Melos 全量中 `subscription-core` 9/9 通过，Flutter App 909 项通过、10 项跳过、11 项既有无关失败，失败仍集中在已记录的 Golden 像素差异及 Collection/Scan 等非埋点用例；Code Review 未发现阻断项。API、Schema、服务端部署和用户操作方式影响为 N/A；真实 dev/prod Mixpanel/Firebase 后台到达仍需真机环境补验。

- 2026-08-27 底部 TabBar Scan 点击区域修复：根因是 64×64 Scan 圆按钮相对 62px TabBar 向上突出 21px，但 Flutter 父级 RenderBox 只在自身边界内执行命中测试，导致按钮顶部 21px 可见却无法点击。现保持 TabBar、圆按钮视觉位置和中间 64px 横向槽位不变，仅在页面层补齐与按钮顶部对齐的 64×21px 透明命中区，并让原 64×64 手势区域完整响应；透明区不提供重复 Semantics，不覆盖 Search 或 Collection。修复前点击按钮顶部 4px 的回归稳定无法进入 Scan，修复后顶部点击、64×21 几何、左右边界无重叠、原 350×62 TabBar 尺寸及 Home→Scan 路由回归通过，`flutter analyze`、Dart 格式和 `git diff --check` 通过。App Shell 8 项中 7 项通过，唯一失败仍为既有 TabBar Golden 5.00% 文字像素差异，未更新基线；Code Review 未发现阻断项。连接中的 iOS 15.6 真机已热重载，当前 Home Widget 树确认原 Scan 按钮与新增透明区同时挂载；iOS 点击手感和 Android 真机仍待人工验收。

- 2026-08-27 Profile Restore 账号验证门禁修复：根因是显式 Restore 在 `AppStore.sync()` 返回 `StoreKit.StoreKitError code=3` 后仍读取设备上的 `Transaction.currentEntitlements`，导致密码错误或取消账号验证时可能复用残留的 verified entitlement 并错误触发 `Premium restored`。现显式 Restore 必须先成功完成 App Store 同步，任意同步取消或失败均停止本次流程，不读取旧 entitlement、不写缓存或 grant，沿用现有 Restore Failed 反馈并保持操作前权益；同步成功后仍按最新 verified current entitlements 区分 Success 与 Not Found。修复前回归稳定得到错误的 Success，修复后订阅/权益相关 6 个测试文件 62/62、Profile Restore 容器行为 1/1、`flutter analyze` 和 Dart 格式通过。扩展 Profile 文件运行中 4 项既有 Golden 像素差异失败，行为测试均通过且本次未修改 UI 或 Golden；Code Review 未发现阻断项。共享 Subscription Page Restore 同步收紧，后台静默 entitlement proof 路径、Workers API、Schema、购买与续订逻辑不变；iOS Sandbox/TestFlight 的正确密码、错误密码和取消三条原始真机路径仍需复验，Android v1.1 未启用订阅销售。

- 2026-08-28 Card Detail Performance Range 页面生命周期修复：根因是 Item Performance 使用按 `collection_item_id` 全局保留的 Riverpod Family Provider，退出详情页后仍缓存用户选择的 Range，导致再次进入同一 Item 时没有恢复 PRD 规定的默认 `1M`。现该 Provider 跟随页面监听生命周期自动释放：同一详情页实例始终保持监听，因此 Collection Item、Performance 与 Price Tab 往返继续保留最后选择；退出详情页释放状态，再次进入同步创建默认 `1M`，旧页面迟到响应不能覆盖新页面。修复前真实路由重进回归在第二次进入时稳定观察到 `1M` 未选中；修复后 Controller 与完整 Card Detail Widget 54/54、`flutter analyze --no-pub`、Dart 格式及 `git diff --check` 通过，Code Review 未发现阻断项。未修改 API、图表数据、Range 请求口径、Price Range 或订阅权限；iOS/Android 真机的返回手势、Tab 往返和重进视觉仍待人工验收。

- 2026-08-28 Search 快捷添加多卡输入草稿切换修复：根因是 Quantity、Purchase Price 与 Notes 使用 `TextFormField.initialValue`，但 Review 切换 Pending Item 时复用了相同的表单 Element 和内部文本 Controller，导致数据层已切到下一条独立草稿，界面仍显示上一条输入；既有测试只断言 Widget 配置的 `initialValue`，未读取实际显示文本，因而遗漏回归。现 Quick Collection Review 按唯一 `pending_item_id` 标识表单 Widget，切换时重新创建三个输入控件并从当前 Item 草稿恢复；Provider 草稿捕获、Portfolio、Finish、卡牌状态、Condition、Grader、Grade、Language、单条/批量保存及 API 契约不变。修复前同一卡牌两条 Pending Item 回归稳定得到第二条错误显示 `3 / 12.34 / first card note`，期望为 `1 / 空 / 空`；修复后两条记录分别恢复自身三项输入，Search 与 Card Detail Widget 91/91、`flutter analyze --no-pub`、Dart 格式及 `git diff --check` 通过，Code Review 未发现阻断项。未执行 iOS/Android 真机的键盘、焦点、滚动位置和快速连续切换验收。

- 2026-08-28 Home Performance Top Performers 排序修复：根因是服务端榜单先按 Profit/Loss 金额排序、Return 百分比仅作为次级条件，与卡片右上角 Return 百分比从大到小的产品口径不一致；Flutter 只接收已截断的前 5 条，因此不能在客户端重排补回被错误截断的条目。现服务端完整榜单先按未舍入的 Return 原值降序，无 Return 的条目置后；Return 相同时继续按 Profit/Loss、Market Value 和 Item ID 稳定排序，再生成首页前 5 条和 View All 的完整 Item ID 顺序。修复前两条回归稳定暴露 Profit/Loss 优先和舍入边界顺序错误，修复后 Performance 定向测试 15/15、Workers type-check 与 `git diff --check` 通过。Workers 全量 67 个文件中 66 个通过、570 项中 569 项通过，唯一失败为本次未改动的 owner-card 并发用例：Wishlist 与 Quick Collect 并发时 `wishlistItemResponse` 读取空记录并返回 500；该文件隔离复跑同样为 4 项中 1 项失败，未将其标记为通过。未修改 Performance 计算、接口字段、Range、Folder、金额隐藏或 Flutter 展示；Code Review 未发现阻断项。dev Worker 已部署为版本 `91075c05-46f4-444d-9a7b-4af68fc642a2`，`https://api-dev.tcgcard.fun/api/v1/health` 返回 `status=ok`；未执行 prod 部署及 iOS/Android 真机数据验收。

- 2026-08-28 Card Detail `Collection Item / Performance / Price` Tab 文字样式修复：Figma `1644:5631` 规定三等分胶囊 Tab 使用完整文案、13px Regular、16px 行高和 `#92927D` 未选中文字；旧实现虽然已有正确的 52px 容器、5px 内边距、42px Tab、边框及选中渐变，但文字写成 15/17，并继承 TabBar 默认左右 label padding，导致 `Collection Item` 可用宽度被额外压缩并出现淡出截断。现移除额外 label padding，统一为 13/16、字重 400、字距 0，选中态继续使用 Accent，未选中态复用页面既有 `#92927D` Figma token；Tab 索引、路由、Performance 加载、Price 和 Collection Item 内容均不变。修复前 390px 回归稳定得到期望 `EdgeInsets.zero`、实际 `null`，修复后完整文案、排版、无超行和切换颜色回归通过，Card Detail Widget 52/52、`flutter analyze --no-pub`、Dart 格式及 `git diff --check` 通过。320px 探索性运行被页面内两个与本次 Tab 无关的既有 Row 溢出阻断（Collection Item 摘要和 Price Range），未标记为通过且未扩大本次范围。Code Review 未发现阻断项；未执行 iOS/Android 真机视觉与系统字体缩放验收。

- 2026-08-31 Search 快捷收藏 Qty 同步修复：复现为已收藏卡牌从 Search `+` 进入 Review 并保存正式 Item，创建成功且详情内部 Quantity 已增加，但返回 Search 后同卡 `Qty` 仍为旧值。根因有三处：单条保存先失效整个 Search Provider，批量保存只启动未等待的二次资产刷新；资产请求失败后又长期缓存失败 Future，后续恢复请求继续复用失败结果；失败兜底使用公共卡牌目录的默认 `Qty=0`，会覆盖页面已确认的持仓值。现单条与批量均使用创建接口返回的正式 `Collection Item`，按同一 `card_ref` 立即合并全部 Folder、Raw/评级等不同版本的 Quantity、Item 数与收藏摘要，同时使旧资产请求失效；后台仍执行权威全量刷新以保留原 Game 与跨页契约，刷新失败时保留当前已确认资产值并清除失败 Future，服务恢复后可重新校准。修复前 Widget 回归稳定得到期望 `Qty: 2`、实际仍为 `Qty: 1`；修复后 Search Controller 34/34、Search Widget 43/43、Set Detail 8/8、Card Detail Widget 51/51、`flutter analyze`、Dart 格式及 `git diff --check` 通过。Code Review 未发现阻断项，确认只合并服务端确认成功项、重复服务端 Item ID 不重复计数，失败或待编辑 Item 不增加 Qty；未修改 Workers API、Schema、收藏口径、权限、价格或 Wishlist 契约。未执行登录态 dev App 原路径、iOS/Android 真机及真实弱网/迟到响应人工验收。

- 2026-08-31 Card Detail 同卡多版本收藏数据展示修复：Collection 列表已把具体 `collection_item_id` 传入详情，Performance 也按该 ID 查询，但 Collection Item 页签固定展示同 `card_ref` 结果集首条，导致先收藏 A 版本数量 1、价格 50，再收藏 B 版本数量 2、价格 13 后，打开 A 可能误显示 B 的数量和收藏价；服务端两条 Item 数据并未因此互相覆盖。现页签在存在 Item 上下文时按 `collection_item_id` 精确选择摘要，编辑和删除继续作用于同一 Item；无 Item 上下文的通用入口及过期 Item 降级保持原行为。修复前双 Item Widget 回归稳定找不到请求的 B 摘要，修复后能独立展示 B 的数量、收藏价和备注且保留 A 数据；Card Detail Controller 与 Widget 95/95、`flutter analyze --no-pub`、Dart 格式及 `git diff --check` 通过。Code Review 未发现阻断项；未修改 Workers API、Schema、保存、唯一性、价格或 Performance 契约，未执行登录态 dev App 原路径及 iOS/Android 真机验收。

- 2026-08-31 Restore 用户取消反馈修复：根因是 iOS 原生桥接把 `AppStore.sync()` 的 `StoreKitError.userCancelled` 与真实错误统一映射为 `apple_restore_failed`，Flutter Controller 又把所有异常统一发送为 `restoreFailed`，因此用户关闭 Apple 系统验证弹窗后错误显示失败 Alert。现原生层按 StoreKit 枚举返回专用 `apple_restore_cancelled`，Flutter 仅对该码静默结束 Loading、保持操作前权益且不增加结果事件；取消及其他同步错误都不会继续读取设备残留 entitlement，网络、账号、Timeout 和其他真实异常仍沿用 Failed。用户原始真机现象与修改前代码路径构成失败证据；修复后 Apple entitlement、Restore 页面和结果弹窗定向 54/54、Profile Restore 单项 1/1、`flutter analyze --no-pub`、iOS 无签名 Debug Build 和限定路径 `git diff --check` 通过。完整 Auth/Profile 文件 68 项中 66 项通过，2 项仍为已记录且与本次逻辑无关的 Profile Banner/Subscription Sheet Golden 像素差异，未覆盖基线。Code Review 未发现阻断项，确认取消不写 Restore 缓存/proof、不执行来源动作，Android 订阅销售路径不变；连接 iPhone 上的 Apple 系统取消原始交互仍需人工复验。

- 2026-08-31 Search 快捷收藏批量删除确认框 UI 对齐：按 Figma `231:2318` 将多卡 Quick Collection Review 的确认框收口为 342×355、16px 圆角的 Danger Modal；产品复核后明确取消渐变背景，最终使用设计右下角采样色 `RGB(29,29,28)`（`#1D1D1C`）作为纯色背景，不再加载背景 SVG 或 PNG。使用设计导出的顶部 26px 与按钮 20px 两套删除 SVG，并按本轮确认移除顶部图标的白色描边；危险标题为 24/32、正文为 15/22，正文水平边距缩至 16px，按钮保持 33px 水平边距与 44px 纵向 `DELETE / CANCEL` 胶囊样式。正文更新为 `This action will permanently delete all these cards and cannot be undone`。320px 小屏下弹窗宽度按 24px 左右安全边距收敛为 272px，正文允许三行并把高度扩展为 377px，不缩小字号或图标。删除确认等待原有本地草稿及 Pending Item 清理完成：等待期间危险按钮显示 Loading、两项操作禁用且弹窗不可返回，成功后才关闭确认框与 Review；取消、清理内容、Provider、API、Schema、批量保存及其他业务逻辑不变。修改前异步确认测试因公共弹窗不支持 `confirmAction` 编译失败；纯色背景测试在修改前稳定找到背景 Image，修改后确认无背景资源 Widget 且容器颜色严格为 `#1D1D1C`。共享 Modal 与完整 Search Widget 48/48、Profile 共用 Danger Modal 1/1、`flutter analyze --no-pub`、Dart 格式及限定路径 `git diff --check` 通过。Code Review 未发现阻断项，确认背景资源已移除，重复点击受保护、Loading 期间取消与系统返回禁用、失败不关闭、空列表自动返回不与确认框抢占路由；iOS/Android 真机视觉仍需人工验收。

- 2026-08-31 Admin 扫描记录环境筛选与展示：共享 PostgreSQL 的 `scan_record` 新增持久化 `environment`，只允许 `development/production`；现有 dev 迁移记录回填为 `development`，新扫描由可信 Worker `APP_ENVIRONMENT` 显式写入，Admin 列表、详情和 `/admin/scans` 筛选均使用该持久化事实。客户端不能自报环境，非法筛选返回 `422`；缺少 Worker 环境配置时返回 `503`、释放已排队扫描额度且不写 R2。Code Review 首轮发现迁移先于部署时旧 Worker 会因非空列无默认值而中断扫描，现为旧 dev Worker 保留 `development` 数据库默认值作为迁移窗口兼容，新 Worker 与未来 prod 导入均禁止依赖该默认值；复审未发现剩余阻断项。Workers 定向 `57/57`、全量 `573/573`、根类型检查 `7/7`、依赖方向、Admin production build、Worker dev dry-run 和 `git diff --check` 通过；Admin 全量 `18/19`，唯一失败仍为既有 `billing-admin-intent.test.mjs` 源码正则断言，与扫描环境路径无关。远程 `0010` 已按 checksum `6ef480092683760fc4f0a50b221ccea35fa58c3ad23c1ebfca30e4272bfde99f` 应用，467 条既有记录全部回填 `development`；dev Worker/Admin version `be0a5923-8c81-485c-b8a3-b0a982fca912` 已承载 100% 流量，health、Admin HTML、10 个静态资源哈希和未授权 API 边界验收通过。未执行 prod 部署、生产写入或真实登录态 Admin 环境筛选人工验收。

- 2026-08-31 Home Performance Tooltip 文本样式按 Figma `1911:8207` 收口：现有 Date、Market、Portfolio、Qty 统一使用 `#999578`、Geist Regular、12px 字号与 18px 行高，并同步调整提示框内部行距和高度以避免文本挤压。Tooltip 字段、金额/数量取值、币种换算、缺失值、Semantics 和交互均不变；PRD 规定不展示的 Daily Change 未新增。Code Review 发现 Home Overview 与 Performance 复用同一 Painter 后，已把新样式严格限定在 Performance 数据行分支并增加 Overview 旧样式保护。Tooltip 相关测试 6/6 与 `flutter analyze --no-pub` 通过；完整 Home Widget 70 项中 69 项通过，唯一失败为既有 Performance Header Golden 10.93% 像素差异，本次未修改或覆盖该基线；iOS/Android 真机视觉仍待人工验收。

- 2026-09-01 Home/Search 首屏卡图启动预加载：现有启动流程仍优先等待 Home 核心数据；核心数据成功后立即在后台预热当前 Folder 实际展示的 Most Valuable 网络卡图，Trending Today 完成后再预热其最多 3 张网络卡图，其他 Folder、Performance 和本地 Asset 不进入该批次。随后在不阻塞进入 Home 的二级页面预热阶段等待 Search 数据；仅当 Search Cards 成功返回且当前默认游戏存在可见卡牌时，按当前排序先截取前 20 条数据，再过滤空图片 URL、去重并写入同一 Flutter 网络图片缓存。空数据、接口失败或图片加载失败均静默跳过，不改 Home/Search 列表、分页、搜索、图片占位或业务状态，也不越过 Search 第 20 条补取后续图片。启动预加载测试 8/8、Home Controller 27/27、Onboarding 启动门禁 5/5、`flutter analyze --no-pub` 与 Dart 格式通过；Code Review 未发现阻断项，确认即使 Home 图片下载未完成也不会延长启动等待，登录会话切换仍走现有重新预加载链路。iOS/Android 真机的冷缓存首屏速度、流量和内存占用仍待人工验收。

- 2026-09-01 Search 当前价格批量查询超限修复：dev 的 Pokemon Search `page=3&page_size=40` 与第 4 页稳定返回裸 500，实时 Worker 日志确认 `Published price query exceeded 1000 rows for 40 cards`；相同数据按 20 卡分页均为 200，根因是当前价格查询固定把 40 个 card ref 作为单块，但这些卡的合法多来源、品相、语言、版本和评级价格维度合计已超过单次 1,000 行安全边界。现保留 40 卡初始分块与 1,000 行上限，多卡查询达到 1,001 行探测值时按 card ref 列表顺序二分并顺序重查，直到每次查询回到边界内；单卡自身仍超限时继续显式失败，不抬高上限、不截断价格、不吞数据库异常。修复前新增回归稳定抛出原异常，修复后递归覆盖 `40→20→10` 且断言所有 card ref 区间恰好查询一次；价格存储、目录适配器、Portfolio 估值与数据路由定向 `63/63`、Workers 全量 `574/574`、Workers type-check 和限定路径 `git diff --check` 通过。当前工作树的 Wrangler remote dev 预览读取同一 dev Hyperdrive/KV 后，第 3、4 页均返回 200、40 条卡牌；该预览未承载正式流量，但 Search read-through 已向共享 dev KV 回填一小时的 `v12` 成功缓存，第 3 页 key 于 `2026-09-01 12:27:06 +08:00` 过期，所以正式 dev 接口随后返回 200 仅是临时缓存命中，不代表旧 Worker 已永久修复。Code Review 未发现阻断项，确认拆分必然收敛、单次响应边界和单卡失败契约保持、共享调用方不依赖跨分块顺序。未修改 API 字段、Schema、migration、缓存键、客户端分页或价格选择口径，未执行 dev/prod 部署或数据库写入；除上述 GET 触发的 dev KV 缓存回填外未执行远程写操作。

- 2026-09-01 官网最新 Figma 样式同步：以 Section `1865:3594` 内 1920px 画板 `1865:3595` 为桌面布局真源，逐段读取 Hero、Features、Steps、Detailed Showcase、Games、CTA、Footer 与 Navigation，并保留窄屏单列响应式。Hero 信任行顶部内距更新为 32.9px，能力卡内距为 25px，Steps 内容增加 24px 水平内缩；三个详情块统一使用 24px 内容分组、16px 列表间距、8px 列表顶部内距和 12px 勾选间距，游戏标签水平内距为 17px。CTA 按原型坐标固定品牌组、标题、说明和下载按钮，Footer 移除双重容器收窄后恢复 x=120；`<=820px` 继续使用无溢出的流式布局。扫描展示替换为 Figma 当前 `1865:3933` 导出，Charizard 使用原始位图独立成层，并按 `1865:3944` 的 2.5 秒线性关键帧循环浮动，整张扫描合成图保持静止；Reduce Motion 直接显示静态合成图。Figma Hero 标题的 4px tracking 与项目 Web 字距必须为 0 的约束冲突，本次明确选择项目约束，不用缩放或字距替代方案静默调和。修改前最新样式契约在旧 Hero/间距实现上失败；修复后 Marketing 3/3、根类型检查 7/7、依赖方向、Wrangler dry-run 和限定路径 `git diff --check` 通过，浏览器在 1920、1440 与 390x844 视口验证顶层高度、CTA/Footer 坐标、扫描图层及水平无溢出。Code Review 修正了图片规则优先级、扫描遮罩边界、Safari 背景模糊和 CTA/Footer 双重收窄。首次部署命令因省略 `run` 被 pnpm 当作内置 deploy，在 Wrangler 启动前失败且无远程影响；随后标准包脚本成功发布，Cloudflare deployment `ee79c2d3-c06b-4595-a433-8fa60d8e128a` 将 version `93561978-f45a-4c70-a6c2-b5f02ae3fc61` 置于 `toccards-website` 100% 流量。绕缓存首页返回 200，CSS 与两张扫描图片的 SHA-256 和本地一致；HTML 包含两处正式 App Store URL、扫描层、CTA 与法律入口，移除 Cloudflare 自动注入的 Web Analytics beacon 及其额外换行后与本地字节一致。正式域名浏览器回读确认七个顶层区块高度与 Figma 基线一致；未修改下载链接、法律内容、API、数据库或其他应用。

- 2026-09-01 官网 Hero Phone Mockups 素材纠正：用户线上截图显示手机组底部区域缺少 Figma 光晕，视觉上像主图被缩小或留下空区。重新读取官网画板 `1865:3595` 的 `Phone Mockups` 子节点 `2792:11227` 后确认布局几何没有变化：导出仍为 `1232×686`，中心手机设计宽度仍为 280px；根因是仓库旧 `hero-phones.png` 与当前 Figma 像素内容不一致，旧 SHA-256 为 `e9a61ba49c2a348614960d39c67ab1e24bf7ea1efb8bcec503a848a8706f3d04`，缺少新导出更强的顶部/底部荧光和完整阴影。现只替换为当前 Figma 原始导出（SHA-256 `098b24f1bdf509e58e600a51496f77920468c060b91e6d95020157630c3aeef0`），不放大图片、不修改 Hero CSS 或其他区块。新增回归在替换前稳定暴露旧哈希，替换后同时保护 PNG、1232×686 尺寸和精确素材身份；Marketing 全量 4/4、Wrangler dry-run 和限定路径 `git diff --check` 通过，本地 1920px 浏览器确认新光晕覆盖原红框区域、图片自然尺寸及页面水平宽度不变。Code Review 未发现剩余阻断项。修复提交 `5517447` 推送后通过标准 Marketing 脚本重新部署，Cloudflare deployment `3df7a233-80d7-414b-ac7c-51e1fb96f44d` 将 version `c114f58a-03cd-4289-bb3f-1852e8344e7a` 置于 `toccards-website` 100% 流量；绕缓存素材返回 200 且远端 SHA-256 与本地一致，正式域名浏览器回读确认新光晕、1232×686 自然尺寸和页面无横向溢出。未修改文案、下载链接、法律内容、API、数据库或其他应用。

- 2026-09-01 dev 后台管理重新发布：从与 `github/dev` 一致的提交 `003d56d50bacf5c151154ceed9db058b84b06203` 执行标准 `deploy:dev`，重新构建 Auth Core 与 Admin development assets，并连同当前 Workers 代码发布到 `toccards-api-dev`。Cloudflare deployment `2cffb572-b56c-4bf4-b593-e83b2f6dd4d5` 将 version `8d2d09f1-7def-4730-be6b-e9c57122ca72`（number `292`）置于 100% dev 流量；回读确认继续使用 dev KV、共享 Hyperdrive、dev R2、`APP_ENVIRONMENT=development`、beta Apple Bundle 与 `cardx.*` Product IDs，未绑定 D1。线上 health 返回 200/`status=ok`，`/admin` 返回 200 HTML，其引用的 10 个 JS/CSS 资源全部可访问且 SHA-256 与本地构建一致，未授权 `/api/v1/admin/scans` 保持 401；此前失败的 Pokemon Search `page=3&page_size=40` 返回 200、40 条卡牌，确认价格查询拆分修复已在正式 dev Worker 生效。发布命令与线上烟测通过；未执行 PostgreSQL migration、prod 部署、生产写入、登录态 Admin 操作或 Flutter 真机验收。

- 2026-09-02 Home Overview/Performance Tab 底部导航恢复修复：根因是 Home 使用 `context.go` 切换底部导航页面时会销毁当前 `HomePage`，而选中的 Performance 仅保存在页面 State 的布尔值中，新 Home 实例固定初始化为 Overview。修复前真实 App Shell 路由回归稳定得到返回后 Performance 字号为未选中的 12px、期望为选中的 16px；现将该选择移入独立的会话级 Riverpod Provider，首次创建仍默认 Overview，Home 与其他底部页面往返后恢复用户最后选择，Performance 数据、Range、Folder、订阅锁定和点击埋点逻辑不变。原失败回归及首次 Overview、切换埋点、Top Performers 返回上下文 4/4 通过；完整 Home Widget 71 项中 68 项通过，3 项未通过均为既有 Home/Performance Golden 像素基线差异，未覆盖基线；`flutter analyze --no-pub` 与 Dart 格式通过。Code Review 未发现阻断项；API、Schema、服务端和其他底部页面状态影响为 N/A，未执行 iOS/Android 真机底部导航人工验收。

- 2026-09-02 Search/Set Detail 快捷收藏 Review/Edit 键盘布局修复：根因是共享新增收藏 Sheet 使用 `AnimatedPadding(bottom=viewInsets)` 避让键盘，导致键盘出现时整个 Review/Edit 连同底部 `TOTAL VALUE` 操作区一起上移并遮挡编辑内容。现仅对快捷收藏 Review/Edit 关闭整层键盘位移，单卡和多卡操作区保持原屏幕底部坐标并由键盘覆盖；编辑滚动区增加与键盘等高的临时滚动余量，键盘打开时把当前焦点字段最小幅度滚动到键盘上方，关闭键盘后移除余量。底部安全距离使用稳定的 `viewPadding`，普通 Card Detail 新增收藏 Sheet 继续沿用原键盘避让。修复前回归稳定测得操作区随键盘上移，修复后单卡/多卡键盘回归 2/2、Search 45/45、Card Detail 54/54、Set Detail 8/8、`flutter analyze --no-pub`、Dart 格式和 `git diff --check` 通过。Code Review 收敛了会重排整个操作区的首版布局，复审未发现阻断项；API、Schema、收藏数据和批量保存行为影响为 N/A，未执行 iOS/Android 真机键盘动画、第三方键盘高度及横屏人工验收。

- 2026-09-02 Home Overview Trending Today 列表项外层样式按 Figma `2165:11956` 收口：旧实现使用 `rgba(255,255,255,0.12)` 纯色背景且没有背景模糊，与设计的暗橄榄玻璃面不一致。现背景精确使用 CSS `139.735930592°` 对应的 Flutter 旋转渐变，从 `rgba(41,43,34,0.4)` 到 `rgba(28,30,21,0.2)`，并在 12px 圆角裁剪内增加 10px backdrop blur；8% 白色边框、12px 圆角和四边 17px 内边距显式保持 Figma 数值。卡图、文案、价格、涨跌、行高、间距、数据和点击逻辑均未修改。修复前局部 Widget 回归稳定得到 `gradient=null`；修复后精确颜色、角度、边框、圆角、内边距和模糊层回归通过，`flutter analyze --no-pub`、Dart 格式及 `git diff --check` 通过。完整 Home Widget 74 项中 71 项通过，3 项未通过为两个整页 Home Golden 与一个无关 Performance Header Golden 的既有工作区像素基线差异，本次未覆盖基线。Code Review 未发现阻断项；API、Schema、服务端和 Trending 数据语义影响为 N/A，未执行 iOS/Android 真机玻璃模糊与不同 GPU 渲染验收。

- 2026-09-02 Card Detail Price 的 `RAW / GRADED` 切换控件按最新 Figma `2363:24457` 补齐分段样式：整体继续保持原位置与 `150×24`，RAW 使用左侧 6px 圆角、GRADED 使用右侧 6px 圆角；两段分别绘制半透明边框、外投影和白色内高光，并以 1px 重叠消除中缝。选中渐变、未选中 Surface 背景、12/16 文字及点击后的状态互换保持既有口径。修改前视觉回归稳定测得 RAW 实际内容仅 `73×22` 且分段自身缺少圆角；修改后默认 RAW 与切换 GRADED 两种状态回归、Card Detail Widget 54/54、`flutter analyze --no-pub`、Dart 格式及限定路径 `git diff --check` 通过。Code Review 未发现阻断项，确认私有控件仅有一个调用点、两段重叠区不存在点击缝隙且未引入平台专用实现。图表、价格数据、Range、Market Prices、订阅权限、API 和 Schema 均未修改；iOS/Android 真机视觉与触控验收尚未执行。

- 2026-09-02 dev 管理后台订阅方案变更 SKU 修复重新部署：从与 `github/dev` 一致的提交 `672935205f8ec0cc7aa744170c4374a3bcb5a718` 创建干净 detached worktree，按锁文件安装依赖并执行标准 `deploy:dev`，重新构建 Auth Core 与 Admin development assets 后连同当前 Workers 代码发布到 `toccards-api-dev`。Cloudflare deployment `2026-09-02T06:26:56.297Z` 将 version `9b1e9696-1bd5-4f2f-b793-94a3e80b0814` 置于 100% dev 流量；绑定回读保持 dev KV、共享 Hyperdrive、dev R2、`APP_ENVIRONMENT=development`、beta Apple Bundle 与 `cardx.*` Product IDs，未绑定 D1。线上 health 返回 200/`status=ok`，Admin SPA 返回 200/`Kando Admin`，HTML 引用的 10 个 JS/CSS 资源逐个返回成功且 SHA-256 与本次构建一致，未授权订单接口保持 401。Admin 静态资源相对前一版本没有变化，本次实际运行更新为包含方案变更目标 SKU 修复的 Worker；未执行 PostgreSQL migration、prod 部署、生产写入或已登录 Admin 操作。真实 `DID_CHANGE_RENEWAL_PREF` 周年切换通知、Lifetime 边界及已处理历史通知不回填仍待 Sandbox 端到端验收。

- 2026-09-02 Search/Set Detail 快捷收藏成功回顶修复：复现为在 Search 结果或 Set 单系列卡牌列表向下滚动后，通过 Review 将全部待编辑卡牌保存成功，弹层关闭且列表数据已使用 `refreshPreservingContent()` 刷新，但页面仍停留在原滚动位置。现两个入口接收 Review 返回的成功数量，仅在全部保存完成并关闭弹层时启动 300ms `easeOutCubic` 平滑回顶；动画与原成功 Toast 并行，取消、删除及部分失败不触发回顶。继续复用既有单次保留内容刷新，不新增请求、不切换整页 Loading，因此刷新期间旧列表保持可见。修复前回归稳定测得 Search/Set 偏移分别停在 210px/1030px，修复后 Search 47/47、Set Detail 8/8、`flutter analyze --no-pub`、Dart 格式及 `git diff --check` 通过。Code Review 未发现阻断项；API、Schema、收藏数据口径、权限和路由影响为 N/A，未执行 iOS/Android 真机的长列表滚动动画与弱网视觉验收。

- 2026-09-03 Subscription Page 前后台 Product 闪动修复：根因是页面实例监听每次 `resumed` 后无条件调用 StoreKit `queryProductDetails`，Controller 同时把共享 `isLoading` 切为 true，导致价格虽仍在内存中，套餐项、Restore 和 Subscribe 按钮仍短暂进入禁用/Loading 状态。现每个新 Subscription Page 实例继续主动校验一次商品；同一实例回前台只读取轻量 storefront 环境指纹，完整目录、配置 SKU 和 storefront 未变化时直接复用已加载 Product，不请求商品且不改变 UI。目录缺失、配置 SKU 或 storefront 变化时允许静默补拉，失败或空结果保留当前已展示目录；购买前按同一 Product/环境契约复核，无效时才进入 Product Loading。全局 Premium entitlement 前台静默刷新保持独立，订阅到期后的原页重新上锁逻辑不变。修改前 Widget 回归稳定得到新页面未校验、第一次 resumed 才刷新；修改后目录复用、不完整目录静默补拉、页面重建、SKU/storefront 变化和 storefront 读取失败降级回归通过，Subscription UI 36/36，订阅/启动门禁/权益组合 70/70，`flutter analyze --no-pub`、Dart 格式及限定路径 `git diff --check` 通过。Code Review 未发现阻断项，确认静默空结果与 storefront 读取失败不会清除已展示 Product，购买前环境变化不会继续使用旧 Product，且未改动 entitlement 前台观察器、API、Schema、StoreKit 商品/购买协议或 Android 未启用销售边界；iOS 真机前后台视频纹理和 storefront 切换尚待人工验收。

- 2026-09-03 Home Most Valuable / Performance Top Performers 涨跌徽标按 Figma `2753:6365` 收口：两处徽标复用同一个样式组件，保留原位置、10/14 文字、`6×2px` 内边距和 4px 圆角，背景从单层 10% Accent 改为其上叠加 30% 黑色，并把 backdrop blur 调整为 12px；正涨幅使用 `#4ADE80`、负涨幅使用 `#FF8989`，零值或缺失值使用 `#FFFFFF`。卡片内容、价格、Most Valuable 30D 涨幅、Top Performers Return 数据口径、排序和点击逻辑均未修改。修复前两处局部回归均稳定测得背景仍为单层 Accent；修复后 Most Valuable 与 Top Performers 样式定向回归、Most Valuable 相关 7 项、市场涨跌颜色 7 项和 `flutter analyze --no-pub` 通过。完整 Home Widget 71 项通过、4 项整页/Performance Golden 失败；完整 Home Golden 包含本次有意视觉变化且还存在更大既有漂移，其余失败为已有视觉基线差异，因此未覆盖基线。Code Review 未发现阻断项，确认两处数据文案仍分别使用原 30D Change 和 Return，仅共享视觉容器；API、Schema、服务端和 Android/iOS 行为分支影响为 N/A，真机不同 GPU 的 backdrop blur 视觉尚待人工验收。

- 2026-09-03 dev 管理后台订阅升级降级订单统计发布：从与 `github/dev` 一致的提交 `67000add08f547cbffc01b1dd8fa8a49a85912c8` 执行标准 `deploy:dev`，重新构建 Auth Core 与 Admin development assets，并连同 Workers 代码发布到 `toccards-api-dev`。Cloudflare deployment `9ab238d2-75df-47de-a8e6-b65cb29e5a89` 将 version `4fea0301-a3a2-46ce-a079-4009c20d81a9` 置于 100% dev 流量；发布回读保持 dev KV、共享 Hyperdrive、dev R2、`APP_ENVIRONMENT=development`、beta Apple Bundle、`cardx.*` Product IDs 和 5 分钟 Cron，未绑定 D1。线上 `/api/v1/health` 返回 200/`status=ok`，`/admin` 返回 200 HTML 与 `Kando Admin` 标题，HTML 引用的 10 个 JS/CSS 均返回 200 且 SHA-256 与本地构建一致，未授权订单接口保持 401。发布前 Workers 全量 67 文件 596 项、Admin 21 项及两侧 type-check 已通过；本次未执行 PostgreSQL migration、prod 部署、生产写入、登录态 Admin 操作或真实 Apple Sandbox/TestFlight 升级降级通知验收。

## 4. 数据库与部署策略

- 已存在的 `0025_billing_admin.sql` 不修改；后续均使用递增迁移。
- owner grant 仅保留兼容旧骨架，授权路径禁止读取；不自动迁移到 session grant。
- 新表/列先向后兼容上线，再切换读写，最后在独立版本清理旧结构。
- 本地、dev、prod 环境严格分离。远程迁移和部署必须单独授权。
- Apple 官方服务端库依赖 Workers `nodejs_compat`，并必须在请求或定时任务处理期间动态加载，避免其依赖在 Worker 全局作用域执行随机操作；dev/prod dry-run build 必须保持通过。
- 每个迁移在对应文档中记录兼容性、回滚方式和数据回填规则。

## 5. 完成定义

“v1.1 开发完成”必须同时满足：三份 PRD 的验收条款均有可追踪证据；评审 P0/P1 全部关闭；客户端与服务端端到端可用；Sandbox/TestFlight 通过；全量测试和构建通过；所有外部配置齐备。单独完成 UI、StoreKit 抽象或 D1 表不能宣称完成。
