default_platform(:ios)

platform :ios do
  desc 'Metadata publishing requires the new App identity and metadata.'
  lane :metadata do
    UI.user_error!('Configure the new App Store identity before publishing metadata.')
  end
end
